---
id: wsm-093
scope: wasm
status: done
depends-on: [wsm-092]
---

# wsm-093 复测公开 -22 / -196：仍挡

## objective

Re-probe the three node-blocked surfaces. Do not open `Sdk.Payments` /
`Sdk.Map`. Do not invent a WAT workaround for `tefBAD_AUTH`.

## 结果（2026-08-31）

| 表面 | 公开 3.3.0-rc1 / 21337 | 本地 2.6.1-rc1 / 63456 |
|---|---|---|
| `pokeEmit` Payment | **tecBYTECODE_REJECTED / -196** | 已绿（`emit.sh`） |
| `pokeSelf` 合约卡 | **-22**，ContractData missing | 已绿（`vault.sh`） |
| Function.ParameterType | Call Parameters 先前已绿 | `sign` 仍 skip |

公开 `pokeBuild` tesSUCCESS/0：host 在，Payment 仍被伪账户 `checkSign` 拒。
rippled 3.3.0：伪账户签名 → `tefBAD_AUTH`（LendingProtocol / BatchV1_1）。
本仓改不了 `alphanet.xrpl.org`。

## 不做什么

- 不开公开 `Sdk.Payments` / `Sdk.Map`
- 不把 -196 当没钱 / 没注资
- 不发明 PDA / `submitTransaction`

## verification

- `XRPL_ALPHANET_RPC=https://alphanet.xrpl.org bash runtime-tests/xrpl/probe-emit.sh`
- `XRPL_ALPHANET_RPC=https://alphanet.xrpl.org bash runtime-tests/xrpl/probe-owner.sh`
- 文档：[xrpl-local.md](../analysis/xrpl-local.md)
