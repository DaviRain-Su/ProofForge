---
id: sf-006
scope: svm-formalization
status: done
depends-on: [sf-000]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-006 TransientModel + Vector64

## 本片交付（2026-09-01）

1. `ProofForge/Svm/Sdk/TransientModel.lean`：`TransientWords` 两槽元数据 + payload
2. `mVec64Begin/Push/Pop/Set/Get/Length/Finish` 与 SDK 同构
3. 定理：stale/full/OOB/empty fail-closed；`mVec64Push_readback`；两 slot 非干涉；begin→finish stale
4. Bytes / Record / Wide 起步同文件（见 sf-007）

## 验收

- `lake build ProofForge.Svm.Sdk.TransientModel` 绿
- SF-4a → done
