---
id: sf-006
scope: svm-formalization
status: todo
depends-on: [sf-000]
plan: ../svm-formalization-plan.md
---

# sf-006 TransientModel + Vector64

## 目标

新建 `ProofForge/Svm/Sdk/TransientModel.lean`：调用期两槽 bump 元数据 + 字数组；证 Vector64 begin/push/pop/set/finish。

## 交付

1. `TransientWords`（或等价）定义；错误码常量对齐 SDK
2. `mVec64Push` / `Pop` / `Set` / `Finish` 与 `TransientVec.lean` 同构
3. 定理：满/OOB/stale fail-closed；两 slot 互不干涉；push 读回

## 非目标

真实堆地址、>2 slot、迭代器。

## 验收

SF-4a → done；ownership 不引入 Emit。
