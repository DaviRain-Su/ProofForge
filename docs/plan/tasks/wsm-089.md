---
id: wsm-089
scope: wasm
status: done
depends-on: [wsm-088]
---

# wsm-089 本地 384-drop 兑付：XrplRate

## objective

Fixed-rate redemption via `Pay.emitToCallerDrops 384`:

- two `credit`s make bal/supp=10
- paused `cash384` returns 4; frozen returns 5
- unpause+unfreeze cash384 burns 10 and pays caller 384 drops net of fee

Else-if, not `&&`. Not Rust `?`. Zero wasm params.
Not `Sdk.Payments`. Not `Sdk.Map`. Public still host -22 and tefBAD_AUTH.

digest `e20892eafa9b3693` (`Examples.XrplRate`). Registry count 58.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 rate 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 58）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/rate.sh`：credit 10；pause cash384 拒 4；unpause freeze cash384 拒 5；unfreeze cash384 A=0 supp=0 caller +384-fee
