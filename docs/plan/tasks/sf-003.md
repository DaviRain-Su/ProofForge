---
id: sf-003
scope: svm-formalization
status: todo
depends-on: [sf-002]
plan: ../svm-formalization-plan.md
---

# sf-003 BoundedVec pop + setAt 读回

## 目标

对称补齐 `mBvPush_*`：`mBvPop_size` / `mBvPop_get`（或等价）与 `mBvSetAt_get`。

## 交付

- 合法 setAt 后同位置读回
- 合法 pop：count-1；返回原末槽值；越界/空 fail-closed 已有则复用
- 不相交：setAt 不改 count header（用 count≠slots）

## 验收

SF-2a → done；StorageModel Vec 区注释标明 push/pop/set 闭环。
