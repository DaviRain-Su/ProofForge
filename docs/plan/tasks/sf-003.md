---
id: sf-003
scope: svm-formalization
status: done
depends-on: [sf-002]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-003 BoundedVec pop + setAt 读回 — done

## 已交付

1. `mBvSetAt_size`：合法 setAt 不改 count header（slots ≠ count）
2. `mBvSetAt_get`：合法 setAt 后同位置 getAt 读回写入值
3. `mBvPop_size`：非空 pop 后 count = size - 1
4. `mBvPop_ret` / `mBvPop_get`：非空 pop 返回值 = 原末槽
5. `mBvPop_oldTail_oob`：pop 后读旧末槽越界得哨兵 0
6. `mBvPush_pop_roundtrip`：未满 push→pop 往返
7. 矩阵 SF-2a → **done**

## 已有（复用）

- `mBvPush_size` / `mBvPush_get` / `mBvPush_twoWrites`
- `mBvSetAt_oob_noop` / `mBvPop_empty_noop` / `mBvGetAt_oob`

## 验收

`lake build ProofForge.Svm.Sdk.StorageModel`；no_sorry / ownership 绿；
StorageModel Vec 区 push/pop/set 闭环。
