---
id: sf-003
scope: svm-formalization
status: done
depends-on: [sf-002]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-003 BoundedVec pop + setAt 读回

## 目标

对称补齐 `mBvPush_*`：`mBvPop_size` / `mBvPop_get`（或等价）与 `mBvSetAt_get`。

## 本片交付（2026-09-01）

1. `mBvSetAt_size`：合法 setAt 不改 count（count≠slots）
2. `mBvSetAt_get`：合法 setAt 后同位置读回
3. `mBvPop_size` / `mBvPop_get`：非空 pop count−1 且返回原末槽值
4. `mBvPush_pop_roundtrip`：未满 push→pop 往返
5. 矩阵 SF-2a → done

## 验收

- `lake build ProofForge.Svm.Sdk.StorageModel` 绿
- StorageModel Vec 区 push/pop/set 闭环
