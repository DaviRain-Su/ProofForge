---
id: l4-034
scope: runtime
status: done
depends-on: [l4-033]
---

# l4-034 关着的格子一次开完

## objective

能 fail-closed 抽出的剩余表面一次铺完。不是 Token-2022，也不是运行时 CPI。

- `Bool` 字段：1 字节 u8-le
- `unixTime`：Clock.unix_timestamp@32，按无符号 u64
- `acc` 上限 0..3
- `systemAdvanceNonce`：System AdvanceNonceAccount tag 4
- `tokenSetAccountAuthority`：SetAuthority AccountOwner
- `tokenInitMultisig`：InitializeMultisig，m=2
- `tokenApprove`：未检查 Approve tag 4

旧 digest 不回退。新例子 Gate / Nonce / TokenOwner / TokenMs。

## path

`Ops.lean`, `Runtime.lean`, `Extract.lean`, `IR.lean`, `Emit.lean`,
`Golden.lean`, `Examples/*`, `Tests/*`, `runtime-tests/solana/tests/*.rs`

## verification

- Gate Mollusk：Bool 读写；`unixTime` 跟 `clock.unix_timestamp`（warp_to_slot 不改 unix）
- Nonce Mollusk：缺 signer Custom(1)。成功路径要现成 nonce 账户，本切片只钉 prelude
- TokenOwner Mollusk：SetAuthority AccountOwner 改 token owner；Approve tag 4 写 delegate
- TokenMs Mollusk：InitializeMultisig2（tag 19，不吃 Rent）m=2 n=2
- Trio digest `70a856aa47c693d9`；Peer / Clock 不回退
- Gate `9183b681f246ac07` / Nonce `5746ebbdd382bd56` / TokenOwner `d29884f00e7311b7` / TokenMs `672b83a54f057f79`
