---
id: l2-002
scope: layout
status: done
depends-on: [l2-001]
---

# l2-002 定长 Array UInt64 n

## objective

Lean 4.31 定长数组是 `Vector UInt64 n`（`Array` 不定长）。`n` 必须是编译期常量。展开成连续 `name_0 … name_{n-1}` 槽，每槽 8 字节。`xs[i]` / `xs.set i v` 的 `i` 必须是字面量且 `i < n`；运行时下标 fail closed。其它元素类型、不定长 `Array` 不做。

## path

`SolanaLean/Extract.lean`, `IR.lean`, `Emit.lean`, `Examples/`（新 fixture）, `Tests/`, `runtime-tests/solana/tests/`

## verification

- 抽出槽数 = n；偏移连续
- 越界投影 fail closed
- Counter / Pair / Flag / Maybe Mollusk 不回退
