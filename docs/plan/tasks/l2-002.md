---
id: l2-002
scope: layout
status: todo
depends-on: [l2-001]
---

# l2-002 定长 Array UInt64 n

## objective

`Array UInt64 n`（`n` 编译期常量）展开成连续 `name_0 … name_{n-1}` 槽，每槽 8 字节。越界 fail closed。其它元素类型不做。

## path

`SolanaLean/Extract.lean`, `IR.lean`, `Emit.lean`, `Examples/`（新 fixture）, `Tests/`, `runtime-tests/solana/tests/`

## verification

- 抽出槽数 = n；偏移连续
- 越界投影 fail closed
- Counter / Pair / Flag / Maybe Mollusk 不回退
