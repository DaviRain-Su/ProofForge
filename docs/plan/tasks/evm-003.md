---
id: evm-003
scope: evm
status: done
depends-on: [evm-002]
---

# evm-003 `#evm_build` + locked solc

## objective

`#evm_build Ns` 复用 `Extract.extractModule`（同一 `@[solana_entry]`），再走 `Evm.IR.fromProgram` + Yul。抽出 digest 必须钉住 `Evm.Golden`。

`lake exe evmLeanAssemble` 写 `.yul` / `.abi.json`，子进程 `solc --strict-assembly --optimize --bin`（要求 0.8.34），解析 `Binary representation:`，写非空 hex `.bin`。PATH 未锁版本不得默默采用。

## path

`SolanaLean/Evm/Commands.lean`, `SolanaLean/Evm/Assemble.lean`, `SolanaLean/Evm/AssembleMain.lean`, `Tests/EvmBuildSpec.lean`, `lakefile.lean`

## verification

- `#evm_build Examples.Counter` 通过；digest 与 fixture 相同
- `#evm_build Examples.Clock` / `Transfer` fail closed
- `lake exe evmLeanAssemble -- build/evm` 写出 `Counter.bin`，hex 非空
