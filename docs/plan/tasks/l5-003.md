---
id: l5-003
scope: runtime
status: doing
depends-on: [l4-016]
---

# l5-003 席位 PDA + vault 初始化

## objective

独立例子 `Examples.Seat`：`createPda` 开席位，`tokenInitAccount` 开 base vault。不跟 Phoenix 挂单混 Program。

双 vault 同一入口、quote vault 第二套 recipe：下一刀。不要发明 remaining-accounts CPI。

## path

`Examples/Seat.lean`, `Tests/SeatSpec.lean`, `Golden.lean`

## verification

- `#pf_build Examples.Seat` 绿
- 抽出 `createPda` / `tokenInitAccount` / `findPda "vault"`
- Mollusk 已覆盖 initialize / PDA view
- **待完成**：`openSeat` / `openBase` CPI 成功路径、权限负例和 quote vault
