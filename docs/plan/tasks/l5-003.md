---
id: l5-003
scope: runtime
status: done
depends-on: [l4-016]
---

# l5-003 席位 PDA + vault 初始化

## objective

独立例子 `Examples.Svm.Seat`：`createPda` 开席位，`tokenInitAccount` 分别开 base / quote vault。不跟 Phoenix 挂单混 Program。

双 vault 同一入口属于下一刀的 Phoenix adapter：需要两组静态账户索引，不发明 remaining-accounts CPI。

## path

`Examples/Svm/Seat.lean`, `Tests/SeatSpec.lean`, `ProofForge/Extract/LegacyGolden.lean`, `runtime-tests/solana/tests/seat.rs`

## verification

- `#pf_build Examples.Svm.Seat` 绿
- 抽出 `createPda` / `tokenInitAccount` / `findPda "vault"`
- Mollusk 覆盖 initialize / PDA view、canonical `openSeat`、base / quote vault 初始化
- 缺 payer signer 和 vault 非 writable 都原子失败；InitializeAccount3 的 owner 不需要 signer
