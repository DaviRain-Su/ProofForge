---
id: evm-006
scope: evm
status: done
depends-on: [evm-005]
---

# evm-006 窄槽 + Option 双叶

## objective

EVM 接受 `UInt8/16/32` 叶子和 `Option UInt64` 双叶。每个叶子仍占一个 storage word；窄值 `and` 低字节。Option：`okState (lit 0)` 清零 tag+payload；其它值写 tag=1 + payload。

Anvil：Flag constructor / setFlag 保 count / getFlag mask；Maybe none 清零、some 写双叶、getValue。

## path

`SolanaLean/Evm/IR.lean`, `Emit.lean`, `Golden.lean`, `Tests/EvmSpec.lean`, `Tests/EvmBuildSpec.lean`, `runtime-tests/evm/anvil_flag.sh`, `runtime-tests/evm/anvil_maybe.sh`

## verification

- `#evm_build Examples.Flag` / `Maybe` 通过
- Flag Yul 含 `and(sload(0), 0xff)`
- Maybe Yul `setSome` 写 `sstore(0, 1)` + `sstore(1, arg0)`；`setNone` 两槽清零
- 本机 Foundry 下两脚本绿
