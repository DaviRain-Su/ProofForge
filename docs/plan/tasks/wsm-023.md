---
id: wsm-023
scope: wasm
status: done
depends-on: [wsm-018]
---

# wsm-023 探针 `cache_le`（AlphaNet，host 存在）

## objective

stdlib：`cache_le(id_ptr, id_len, cache_num) → i32`。活网 import **能实例化**
（`rE2LaDjccsJwx21ebjeVEUuqJ5GfkDxSZV`）。零 32B id + `cache_num=0` 调用
`tecBYTECODE_REJECTED` / VM **-10**（LedgerObjNotFound）。那是「host 跑了」，
不是 AccountRoot.Balance。

**不开 `Sdk.AccountRoot`。** 下一刀才是真实 keylet + `le_field(Balance)`。

## 不做什么

- 不把 -10 当绿的余额读
- 不抄 EVM `balance(address)`
- 不进 CI

## path

`runtime-tests/xrpl/fixture/probe-cache.wat`，`runtime-tests/xrpl/probe-cache.sh`

## verification

- `ContractCreate` 含 `cache_le` import 成功
- poke 返回 host 错误码（-10），不是 missing-import
