---
id: wsm-071
scope: wasm
status: done
depends-on: [wsm-070]
---

# wsm-071 本地 allowance：grant / takeB

## objective

Compile-time spender (wallet B) pulls 5 from A's card:

- caller `grant` writes `allw=5` on this card
- B `takeB` requires `allw≥5` and `bal≥5` on A, credits B, cuts `allw`
- freeze take returns 5; pause take returns 4; spent allw returns 1

SDK: `Card.peekCallerAllw` / `Card.flushCallerAllw` unfold existing Runtime.
Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Public still host -22 and tefBAD_AUTH.

digest `e31f80dc4c97ee66` (`Examples.XrplTake`). Registry count 40.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 AccountID 参数
- 不把本地 take 写成公开活网验收
- 不叫 ERC-20 `transferFrom`

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 40）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/take.sh`：take 无 allw 溢 1；credit 10；freeze take 拒 5；pause take 拒 4；grant；takeB A=5 B=5 allw=0；再 take 溢 1
