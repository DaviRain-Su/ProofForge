---
id: wsm-040
scope: wasm
status: done
depends-on: [wsm-039]
---

# wsm-040 cross-wallet operator

`Examples.XrplCrew` digest `ca03e80ef4a8218a`. Same shared-card trick as
`XrplHand`: `accountLit` genesis rewrites mem[0..19]. Second wallet deploys
as owner, `setOp` writes genesis as operator, genesis `bump`s. Pre-setOp
genesis bump returns 3. Live `crew.sh`. Not EVM Roles bitmap.
