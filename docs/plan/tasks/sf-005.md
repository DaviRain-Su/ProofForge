---
id: sf-005
scope: svm-formalization
status: done
depends-on: [sf-003]
plan: ../svm-formalization-plan.md
---

# sf-005 StorageBitSet mask 代数 + 账户桥

## 目标

先证纯函数 `containsOf/insertOf/removeOf/toggleOf` 的位级性质，再桥到 AccountWords 单字读写。

## 交付

1. L1：`BitSet.wellFormed` parts；`wordCount` 几何
2. 纯：insert 后 contains；remove 后 not；toggle 对合；OOB 无写
3. L2：`mBitSetInsert_readback` 等（单字 touch）

可对齐 `Core.Collections` BoundedBitSet 政策，但不强行合并物理布局。

## 验收

SF-3 → done。
