---
id: sf-005
scope: svm-formalization
status: done
depends-on: [sf-003]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-005 StorageBitSet mask 代数 + 账户桥

## 目标

先证纯函数 `containsOf/insertOf/removeOf/toggleOf` 的位级性质，再桥到 AccountWords 单字读写。

## 本片交付（2026-09-01）

1. L1：`bitSet_wf_parts` / `wordCount_eq` / `mFieldWord_bitSet_words`
2. 纯：`insertOf_contains` / `removeOf_not_contains` / `toggleOf_involutive` + mask 辅助
3. L2：`mBitSetContains|Insert|Remove|Toggle` 与 OOB noop；`mBitSetInsert_absent_readback` / `mBitSetRemove_present_readback`
4. 模块：`ProofForge/Svm/Sdk/StorageBitSetModel.lean`
5. 矩阵 SF-3 → done

## 验收

- `lake build ProofForge.Svm.Sdk.StorageBitSetModel` 绿
