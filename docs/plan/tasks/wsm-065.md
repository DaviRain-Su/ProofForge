---
id: wsm-065
scope: wasm
status: done
depends-on: [wsm-064]
---

# wsm-065 本地冻结门金库（XrplSafe）

## objective

Add per-user `lock` on top of XrplBank. Frozen `cash` returns status 5
and leaves `bal`/`supp`. `credit` still works while frozen. Pause still
blocks credit/cash (4). Unfreeze then cash burns 5/5 and emits 192 drops.

Not `Sdk.Payments`. Not `Sdk.Map`. Public AlphaNet still -22 / -196.

digest `317c295ada5d467c` (`Examples.XrplSafe`). Registry count 34.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明 PDA
- 不把本地冻结金库写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 34）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/safe.sh`：credit 5/5；freeze cash=5；pause credit/cash=4；unpause；cash 0/0
