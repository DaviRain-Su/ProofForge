---
id: wsm-057
scope: wasm
status: done
depends-on: [wsm-056]
---

# wsm-057 minter `clawback` 源卡

## objective

Minter burns `amount` from a parameter AccountID card without `allw`.
Works on a frozen card. Decrements minter `supp`, then restores caller
before persist.

digest `86625b1e737a9f82`.

- 非 minter → 3
- 暂停 → 4
- 冻结不挡
- 缺余额 / supp → 1
- 成功：源 `bal` 减、`supp` 减；minter `bal` 不变

## verification

- `lake build Tests.XrplSpec`
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/mint.sh` 尾部：B freeze；B clawback 拒 3；A clawback(B,1) → A=1 B=5 supp=6
