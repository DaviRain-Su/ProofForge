---
id: wsm-088
scope: wasm
status: done
depends-on: [wsm-087]
---

# wsm-088 本地 cap + operator 双向锁：XrplCrimp

## objective

SDK `Card.flushCapLit` / `flushAllwLit` plus dual timelock:

- `setCap10` then third `credit` returns 1
- ungranted B `pause` returns 3; after `grantOp` B pause then lockIn returns 4
- A unpause then cancel before due (`due = ledgerSqn + 2`); after due only B cashB +192

No freeze pair (export budget). Else-if, not `||` / `&&`. Not Rust `?`.
Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Public still host -22 and tefBAD_AUTH.

digest `bae04d6719fca728` (`Examples.XrplCrimp`). Registry count 57.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 crimp 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 57）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/crimp.sh`：credit 10；setCap10；credit 拒 1；B pause 拒 3；grantOp；B pause lockIn 拒 4；A unpause lockIn；cancel；lockIn；early B cash 拒 1；A cash 拒 3；late A cancel 拒 1；B cashB +192
