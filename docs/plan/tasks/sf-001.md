---
id: sf-001
scope: svm-formalization
status: doing
depends-on: [sf-000]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-001 Queue 非空 push（收口余量）

## 已在 main 落地（勿重复劳动）

- `mQueuePush_empty_links` / `mQueuePush_empty_readback`
- `mQueuePush_nowrap_links`（head≠0 且 head+size≤cap → 三写组合）
- 通用 `u64toNatAdd` 无回绕桥

## 本片仍要交

1. `mQueuePush_wrap_links`：环绕分支 `tail = head + size - cap` 链接到 `mQueuePushAt`
2. 非空 push **读回**（nowrap 与 wrap 各一，或统一读回引理）
3. 矩阵 SF-1a → done

## 手法

沿用 main 的 `u64toNatAdd/Sub` + 三词分离 + `mQueuePushAt_twoWrites`。

## 验收

StorageModel kernel 过；no_sorry / ownership 绿；不漂移既有 empty/nowrap 定理。
