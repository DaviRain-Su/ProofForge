---
id: sf-001
scope: svm-formalization
status: done
depends-on: [sf-000]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-001 Queue 非空 push（收口）— done

## 已交付（本分支）

1. `mQueuePush_wrap_links`：环绕分支 `tail = head + size - cap` 链接到 `mQueuePushAt`
2. `mQueuePush_wrap_tail_bounds`：wrap 下 tail ∈ `[1, capacity]`
3. `mQueuePush_nowrap_readback` / `mQueuePush_wrap_readback`：非空 push 读回
4. 矩阵 SF-1a → **done**

## 早已在 main（勿重复）

- `mQueuePush_empty_links` / `mQueuePush_empty_readback`
- `mQueuePush_nowrap_links`（head≠0 且 head+size≤cap → 三写组合）
- 通用 `u64toNatAdd` 无回绕桥

## 验收

`lake build ProofForge.Svm.Sdk.StorageModel`；`scripts/check_no_sorry.py` /
`scripts/check_ownership.py` 绿；未漂移 empty/nowrap 定理。
