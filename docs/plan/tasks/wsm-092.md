---
id: wsm-092
scope: wasm
status: done
depends-on: [wsm-091]
---

# wsm-092 本地 revoke 清 allowance：XrplRevoke

## objective

Crate path plus A `revoke` (`allw=0`):

- ungranted / revoked B `takeAndLock` returns 1
- after a live `grant`, B `takeAndLock` locks 5 into `esc` with due+2
- A cancel before due; after due only B cashB +192

Else-if, not `&&`. Not Rust `?`. Zero wasm params.
Not `Sdk.Payments`. Not `Sdk.Map`. Public still host -22 and tefBAD_AUTH.

digest `67d5b2529f86c2c7` (`Examples.XrplRevoke`). Registry count 61.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 revoke 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 61）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/revoke.sh`：credit 10；grant；revoke；B takeAndLock 拒 1 allw=0；grant；takeAndLock A=5 esc=5；cancel；grant；takeAndLock；early B cash 拒 1；A cash 拒 3；late A cancel 拒 1；B cashB +192
