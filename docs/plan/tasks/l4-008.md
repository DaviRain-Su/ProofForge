---
id: l4-008
scope: runtime
status: done
depends-on: [l4-007]
---

# l4-008 System createAccount

## objective

`systemCreate lamports space` 是普通 Lean 包装，抽出成已有 `invoke`。

- 外层：payer s+w、新账户 s+w、System
- 内层：`u32le(0) || u64le lamports || u64le space || owner32`
- `owner32` 本切片钉死为当前 program id（新 `CpiWord.programId`）
- 负例：新账户缺 signer
- 不再复制发射器

## path

`Ops.lean`, `Runtime.lean`, `Extract.lean`, `Emit.lean`, `Golden.lean`, `Examples/Create.lean`, `Tests/*`, `runtime-tests/solana/tests/create.rs`

## verification

- Create Mollusk：账户被创建，owner = 当前 program，lamports/space 对得上
- 缺新账户 signer → Custom(1)
- Transfer / Signed 不回退
