---
id: l3-002
scope: shape
status: done
depends-on: [l3-001]
---

# l3-002 Option match 读 payload

## objective

`match s.slot with | none => 0 | some n => n` 抽成 `ite (eq tag 0) (return 0) (return payload)`。只开 `Option UInt64` 两支。用户 inductive 带字段仍 fail closed。

## path

`ProofForge/Extract.lean`, `Examples/Maybe.lean`, `Tests/*`, `runtime-tests/solana/tests/maybe.rs`

## verification

- `getValue` none → 0；some 77 → 77
- 不改账户
- 带 payload 用户枚举仍 fail closed
- Counter / Pair / Flag / Window / Phase 不回退
