---
id: wsm-103
scope: wasm
status: done
depends-on: [wsm-061]
---

# wsm-103 本地状态金额 Payment：XrplDraw

## objective

`Pay.emitToCallerDrops` 叶和 `xpayd` 发射器已有，但 `XrplTip` 用的是字面量 384。
本刀证明操作数可以是 **State 字段** `s.bal`（不是 wasm 参数，本地 2.6.1 仍零参）。

digest `169b4a3422828871`（`Examples.XrplDraw`）。Registry count 68。
公开仍 **-196**。不是 `Sdk.Payments`。

## 不做什么

- 不开公开 `Sdk.Payments`
- 不改 Emit / Host / Extract
- 不把本地绿写成公开验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 68）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/draw.sh`：nid≠21337；credit bal=192；draw tesSUCCESS/0 且 bal=0
