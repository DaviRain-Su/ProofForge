---
id: sf-016
scope: svm-formalization
status: todo
depends-on: [sf-002, sf-004, sf-005, sf-007, sf-009, sf-010, sf-011, sf-012, sf-013, sf-014, sf-015]
plan: ../svm-formalization-plan.md
---

# sf-016 SVM 形式化收口审计

## 目标

确认 §6 矩阵全绿，宣布 SVM 组件 L1/L2 形式化收口。

## 交付

1. 逐行审计 `docs/plan/svm-formalization-plan.md` §6
2. 汇总定理数 / 文件表写入该计划「收口记录」小节
3. `lake build` + `python3 scripts/check_no_sorry.py` + `python3 scripts/check_ownership.py`
4. 抽查：每个 SDK 模块至少有 L1 或显式 `n/a`；每个持久容器有 L2 或书面豁免
5. 更新 `docs/plan/README.md` / `docs/INDEX.md` 指向「SVM L1/L2 已收口」
6. **不**合并 WASM PR；只在计划中重申其仍开放

## 验收

SF-10 → done；本 task → done。
