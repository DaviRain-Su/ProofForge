---
id: wsm-038
scope: wasm
status: done
depends-on: [wsm-037]
---

# wsm-038 TwoStep Ownable + dual role + litBalance + txFlags

组合已接线的 Runtime / SDK，不新 host、不新 Op：

| 合约 | 能力 |
|---|---|
| `XrplStep` | pending 三叶；`propose` 记 caller；`accept` 把 pending 写成 owner |
| `XrplRole` | owner + operator；`setOp` owner-only；`bump` 走 `requireOwnerOr` |
| `XrplPeer` | `Context.litBalanceDrops hex` 读编译期 AccountID 的 AccountRoot.Balance；persist owner 仍是 caller |
| `XrplFlag` | `Context.txFlags` ← `tx_field(sfFlags=131074)` |

活网零参数。Step/Role 同一把钥匙自提名。Peer 第二把钥匙
`d0bc2a540b15411f44a24dfb58d23ad5d9d9b350`。不是 Sdk.Map / Sdk.Amm。

digest：Step `8273bd4064e4745a` · Role `bae46704480482ee` · Peer `b808c0cc3278fb10` · Flag `d71a13301ce82878`。
