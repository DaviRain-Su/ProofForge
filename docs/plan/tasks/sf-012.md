---
id: sf-012
scope: svm-formalization
status: doing
depends-on: [sf-009]
plan: ../svm-formalization-plan.md
---

# sf-012 FifoCancel 有界折料

## 目标

在 AccountWords + 既有 map/cursor 引理上，证 FifoCancel 有界循环的关键代数（不是协议正确性）。

## 交付

1. 游标步进：每次 cancel 后 cursor/map 与模型一致（N≤固定上界或归纳骨架）
2. fail-closed：校验失败不改持久字
3. 与 Sdk facade 同名操作的定义对齐说明

## 进度

已落地（纯 L2 折料骨架，`ProofForge/Svm/Sdk/FifoCancelModel.lean`）：

* `OrderBook` / `cancelStep` / `cancelGo` / `cancelUpTo`
* fail-closed：`trader = 0` / `searchLimit = 0` / `cancelLimit = 0` → rewind 初态
* 预算不变量：`canceled ≤ cancelLimit`、`searched ≤ searchLimit`
* 空簿保持；step 引理（end / skip-owner / cancel）
* 与 `FifoCancel.Call.cancelUpTo` 标量预算对齐说明（AccountWords 桥待续）

待补：AccountWords / FIFO cursor 字段 ↔ `OrderBook.cursor` 桥；collateral 释放折叠。

## 非目标

venue fee/match semantics.

## 验收

SF-8a → done（骨架已开；AccountWords 桥闭合后转 done）。
