---
id: sf-012
scope: svm-formalization
status: done
depends-on: [sf-009]
plan: ../svm-formalization-plan.md
---

# sf-012 FifoCancel 有界折料

## 目标

在 AccountWords + 既有 map/cursor 引理上，证 FifoCancel 有界循环的关键代数（不是协议正确性）。

## 交付

1. 游标步进：每次 cancel 后 cursor/map 与模型一致 — **done**（`OrderBook` + `cancelGo`/`cancelUpTo` + AccountWords `loadCursor`/`storeCursor`）
2. fail-closed：校验失败不改持久字 — **done**
3. 与 Sdk facade 同名操作的定义对齐说明 — **done**

## 非目标

Venue 费用/撮合语义；collateral 释放折叠（可后续加厚）。

## 验收

SF-8a → done。
