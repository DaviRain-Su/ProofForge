---
id: wsm-055
scope: wasm
status: done
depends-on: [wsm-054]
---

# wsm-055 minter `freezeOf` 对方卡

## objective

Minter 把 `lock=1/0` 写到参数三叶那张用户卡上。不是 PDA，不是程序金库。

digest `3aeac2fd4e14b8c5`。

- `freezeOf(w0,w1,w2)` / `unfreezeOf`：`requireOwner minter`
- 非 minter → 状态码 3
- 写完 dest `lock` 后 `storeOwnerLimbs caller`，再 persist `s.bal`（否则 minter `$bal` 拷到 dest）
- 已冻结 dest 的 `pay` → 5，卡片不变

## verification

- `lake build Tests.XrplSpec`
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/mint.sh` 尾部：A freezeOf(B) → B.lock=1；A pay(B) → 5；B freezeOf 拒 3；A unfreezeOf → B.lock=0；A=2 B=6
