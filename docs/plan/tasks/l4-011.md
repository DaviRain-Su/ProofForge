---
id: l4-011
scope: runtime
status: done
depends-on: [l4-010]
---

# l4-011 rentExemption

## objective

`rentExemption dataLen` 是普通 Lean 叶子，抽出后读 Rent sysvar。

- syscall：`sol_get_rent_sysvar`
- 读 `lamports_per_byte`（偏移 0）
- 链上：`rate * (128 + dataLen)`
- `dataLen` 抽出时必须是常量；本切片钉死 16
- 不读 `exemption_threshold` 浮点
- 宿主 `@[irreducible]` stub

## path

`Ops.lean`, `Runtime.lean`, `Extract.lean`, `Emit.lean`, `IR.lean`, `Golden.lean`, `Examples/Rent.lean`, `Tests/*`, `runtime-tests/solana/tests/rent.rs`

## verification

- Rent Mollusk：`exempt` 返回 `mollusk.sysvars.rent.minimum_balance(16)`
- 改 `lamports_per_byte` 后再读，值跟着变
- Clock digest 不回退
