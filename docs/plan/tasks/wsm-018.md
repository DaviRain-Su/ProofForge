---
id: wsm-018
scope: wasm
status: planned
depends-on: [wsm-017]
---

# wsm-018 后续排期：复杂合约缺口（文档）

## objective

Counter / Ownable / Pausable / hash 组合已经齐。再加 SDK 空名变不出
Uniswap。权威下一阶段见 [xrpl-next.md](../analysis/xrpl-next.md)。

本卡只把那份文档挂进任务表，**不写代码**。下一刀代码是
**wsm-021 `trace_num` 探针** 或 **wsm-023 `cache_le` 探针**（二选一，探针绿才开 Runtime）。

## 不做什么

- 不在本卡实现 Map / Payments / 用户 ContractData
- 不推代码抢 `wasm-near` 的 rebase 窗口（本卡落地时 `wasm-feature` 停在 `190f543`）
- 不把 EVM `Storage.Map` / SVM RBMap 接到 XRPL

## path

`docs/plan/analysis/xrpl-next.md`，`docs/plan/README.md`，
`docs/plan/analysis/xrpl-sdk-gap.md`

## verification

- `xrpl-next.md` 列出 A（组合）/ B（探针）/ C（存储）/ D（账本效应）四段
- 写明 wsm-026 用户 ContractData 是 Uniswap 式余额的最小前置
- 写明没有叶子就不要 `Sdk.Map` / `Sdk.Payments`
