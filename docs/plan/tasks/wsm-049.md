---
id: wsm-049
scope: wasm
status: done
depends-on: [wsm-048]
---

# wsm-049 takeFrom 自卡别名

源卡就是 caller 时只扣 `allw`，不 debit/credit `bal`。不是 Map。

`Examples.XrplMint` digest `741c2c1cedb1c98b`。

活网：B `approve(1)` 后 `takeFrom(B,1)` → B.bal 仍 8，B.allw=0，A.bal 仍 1。
