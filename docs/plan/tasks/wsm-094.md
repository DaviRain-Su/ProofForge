---
id: wsm-094
scope: wasm
status: done
depends-on: [wsm-093]
---

# wsm-094 本地 send/claw + cashSelf：XrplReed

## objective

Compose sendToB / clawB with freeze-gated `cashSelf` (192 drops):

- credit 15; freeze send returns 5; unfreeze sendToB A=10 B=5
- B claw returns 3; A clawB B=0 supp=10
- freeze cashSelf returns 5; unfreeze cashSelf A=5 supp=5 caller +192-fee

Else-if, not `&&`. Not Rust `?`. Zero wasm params.
Not `Sdk.Payments`. Not `Sdk.Map`. Public still host -22 and tefBAD_AUTH.

digest `852be7802d80e57a` (`Examples.XrplReed`). Registry count 62.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 reed 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 62）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/reed.sh`：上列断言
