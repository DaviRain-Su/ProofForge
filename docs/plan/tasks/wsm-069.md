---
id: wsm-069
scope: wasm
status: done
depends-on: [wsm-068]
---

# wsm-069 本地 token：pause / freeze + treasury

## objective

XrplTreasury plus:

- minter `pause` / `unpause` (`halt` on the minter card)
- caller `freeze` / `unfreeze` (`lock` on this card)
- paused credit returns 4; frozen sendToB returns 5 and does not debit

Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Public still host -22 and tefBAD_AUTH.

digest `d03a887e6b52e7a8` (`Examples.XrplToken`). Registry count 38.
Create on 2.6.1 rejects 14-export Functions (`temARRAY_TOO_LARGE`); this
program keeps 11 exports. Cap/operator stay on `XrplFund`.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 AccountID 参数
- 不把本地 token 写成公开活网验收
- 不叫 `Sdk.Token` / ERC-20

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 38）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/token.sh`：credit 10；freeze send 拒 5；unfreeze；pause credit 拒 4；unpause；sendToB A=5 B=5；clawB B=0 supp=5；burn A=0 supp=0；credit；cashSelf A 净得 192 drops（扣 1e6 Call 费）
