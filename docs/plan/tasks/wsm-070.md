---
id: wsm-070
scope: wasm
status: done
depends-on: [wsm-069]
---

# wsm-070 本地 share：mintToB / cashToB + Card.persistCaller

## objective

XrplToken plus:

- SDK `Card.persistCaller` / `Card.peekSelfSupp` (unfold existing Runtime)
- minter `mintToB` credits 5 onto wallet B and bumps contract `supp`
- caller `cashToB` burns 5 and emits 192 drops to B
- pause mint returns 4; freeze send returns 5

Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Public still host -22 and tefBAD_AUTH.

digest `e53efc71c6393ba4` (`Examples.XrplShare`). Registry count 39.
Create on 2.6.1 rejects 14-export Functions (`temARRAY_TOO_LARGE`); this
program keeps 11 exports.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 AccountID 参数
- 不把本地 share 写成公开活网验收
- 不叫 `Sdk.Token` / ERC-20

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 39）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/share.sh`：mintToB B=5 A=0 supp=5；credit A=5 supp=10；freeze send 拒 5；pause mint 拒 4；clawB B=0 supp=5；cashToB A=0 supp=0 B.xrp+=192
