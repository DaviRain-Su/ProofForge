---
id: wsm-080
scope: wasm
status: done
depends-on: [wsm-079]
---

# wsm-080 本地冻结门双向锁：XrplGlaze

## objective

SDK `Card.flushCallerLock` plus freeze-gated dual timelock:

- `lockIn` moves 5 onto contract `esc` with `due = ledgerSqn + 5`
- frozen A `cancel` / frozen B `cashB` return 5
- unfreeze then A cancel before due; after due only B cashB +192

Uses `Card.callerUnlocked` / `Card.flushCallerLock` / `Gate.ok`.
Not `&&`. Not Rust `?`. Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Public still host -22 and tefBAD_AUTH.

digest `3775987a7dfab228` (`Examples.XrplGlaze`). Registry count 49.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 glaze 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 49）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/glaze.sh`：credit 10；lockIn；freeze cancel 拒 5；unfreeze cancel；lockIn；freeze B cash 拒 5；unfreeze early B cash 拒 1；A cash 拒 3；late A cancel 拒 1；B cashB +192
