---
id: l5-001
scope: extract
status: done
depends-on: [l4-034]
---

# l5-001 有界 for 改状态

## objective

`for i in [:n]` 抽出 `Op.forBody n body`，体里可用 `loopIx`。体里 `okState` / `invoke` / `errorOverflow` 提前结束；跑完落到循环后的 op。

`forAccum` 仍只做累加 view。不要把累加收成 `forBody`。

## path

`Ops.lean`, `Extract.lean`, `Svm/Emit.lean`, `Evm/Emit.lean`, `Examples/Svm/Book.lean`

## verification

- `Examples.Svm.Book.fillFirst` 抽出 `forBody 4 [ite … indexSet …]`
- `#pf_build Examples.Svm.Book` 绿
- `setAt` / `sum4` digest 不回退
