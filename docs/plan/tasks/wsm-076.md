---
id: wsm-076
scope: wasm
status: done
depends-on: [wsm-075]
---

# wsm-076 本地双向时间锁：A cancel / B claimB

## objective

XrplClaim plus A cancel before due:

- A `lockIn` moves 5 onto contract `esc`, `due = ledgerSqn + 2`
- A `cancel` before due returns esc to A
- after due A cancel returns 1; only wallet B `claimB`
- early B claim returns 1; A calling claimB returns 3

Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Not XRPL EscrowCancel. Public still host -22 and tefBAD_AUTH.

digest `d8b9fb0c4ce39299` (`Examples.XrplDual`). Registry count 45.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 AccountID 参数
- 不把本地 dual 写成公开活网验收
- 不叫 XRPL 原生 EscrowCancel

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 45）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/dual.sh`：credit 10；lockIn；A cancel A=10 esc=0；lockIn；立刻 B claim 拒 1；A claimB 拒 3；A cancel 拒 1；B claimB B=5 esc=0 A=5
