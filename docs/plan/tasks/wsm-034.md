---
id: wsm-034
scope: wasm
status: done
depends-on: [wsm-030]
---

# wsm-034 AccountRoot Sequence / Flags / OwnerCount

## objective

同一条已绿的 `accountroot_id` + `cache_le` + `le_field` 路径，再读三个 UInt32：

| 叶 | sfield | 编码 |
|---|---|---|
| `xrplCallerSequence` | 131076 | LE UInt32 → UInt64 |
| `xrplCallerFlags` | 131074 | 同上 |
| `xrplCallerOwnerCount` | 131085 | cache_le 快照；随后写 ContractData 会 +1 |

`Examples.XrplRoot` digest `a8e6569035ec2d13`。活网 `seq` 对上 `account_info.Sequence`。

## 注资（wsm-030 续）

AlphaNet `3.3.0-rc1`：

- Create `InstanceParameters`（含 UINT64 flag 0 和 `tfSendAmount`）→ **temMALFORMED**
- Call `Parameters` AMOUNT `tfSendAmount` → **temMALFORMED**
- Function ABI 上声明 `tfSendAmount` 可以 Create，但 Call 带值仍 malformed
- 普通 Payment 打合约 → **tecNO_PERMISSION**
- `emit_built_txn` Payment → **-196 tecPSEUDO_ACCOUNT**
- `BatchV1_1` **已开**

伪账户没有资金路径。不开 `Sdk.Payments`。

## 不做什么

- 不发 Payment、不开 Amm
- 不进 CI 活网
