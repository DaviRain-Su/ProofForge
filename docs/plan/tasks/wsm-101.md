---
id: wsm-101
scope: wasm
status: done
depends-on: [wsm-100]
---

# wsm-101 公开 AlphaNet：真实 RippleState 读路径

## objective

wsm-100 缺 SLE 是 **-10**。本刀先发 **TrustSet**（节点交易，不是 IR），再走
`trustline_id → cache_le → le_field(Balance)`。**不开 Sdk.Trustline / Runtime 叶。**

## 结果（2026-08-31，公开 3.3.0-rc1 / 21337）

- TrustSet USD / issuer B `rLpgximdBvEHy8TxUwyj6mjCRNcJju5qGG` tesSUCCESS
- RippleState 存在：`LowLimit` USD/genesis 1000000，`Balance` 0
- 合约 poke **tesSUCCESS / vmReturnCode=0**
- caller 卡 `drops` = `0x8000000000000000`（零 IOU STAmount 前 8 字节，高位 0x80）
- 缺 SLE 对照（ISO ZZZ）：仍 **-10**，没有把负码写成 0

20-byte 布局（下一刀 Runtime 用）：

- currency USD：`0000000000000000000000005553440000000000`
- issuer B：`d0bc2a540b15411f44a24dfb58d23ad5d9d9b350`

## 不做什么

- 不开 `Sdk.Trustline`
- 不把 `0x8000…` 解码成 drops 十进制（那是 IOU STAmount，不是 XRP 57-bit）
- 不进 CI 活网

## path

`runtime-tests/xrpl/alphanet-rpc.js`（`trustset` / `pay_iou` / `line`），
`fixture/probe-line.wat`，`probe-line.sh`

## verification

- `XRPL_ALPHANET_RPC=https://alphanet.xrpl.org bash runtime-tests/xrpl/probe-line.sh`
- poke tesSUCCESS/0 且 slot `drops` 非空；ZZZ 对照 -10
