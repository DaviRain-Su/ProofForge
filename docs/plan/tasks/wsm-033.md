---
id: wsm-033
scope: wasm
status: done
depends-on: [wsm-029]
---

# wsm-033 Runtime 叶：caller XRP drops

## objective

把 wsm-029 探针接到 Lean：

- `Runtime.xrplCallerBalanceDrops`
- `Sdk.Context.callerBalanceDrops`
- WAT（仅 AlphaNet host）：`accountroot_id` + `cache_le` + `le_field(393218)`，
  STAmount 大端 u64 `& 0x01FFFFFFFFFFFFFF`

`Examples.XrplBalRt` digest `dd80a5af3243dec2`。活网创世账户 drops =
`100000000000000`。Bedrock 本地这些 import 名为空，不发射。

## 不做什么

- 不发 Payment、不读别人账户、不开 `Sdk.Amm`
- 不进 CI 活网
