---
id: wsm-063
scope: wasm
status: done
depends-on: [wsm-062]
---

# wsm-063 本地程序卡积分兑 XRP（XrplCash）

## objective

Compose already-green local leaves: `Card.storeSelf` + `Pay.emitToCaller`.
`credit` writes caller `bal=5` and contract `supp=5`. `cash` burns both
and emits 192 drops to the caller.

Not `Sdk.Payments`. Not `Sdk.Map`. Public AlphaNet still -22 / -196.

digest `86367a05030e0c5a` (`Examples.XrplCash`). Registry count 32.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明 PDA
- 不把本地兑 XRP 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 32）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/cash.sh`：credit bal=5/supp=5；cash bal=0/supp=0 tesSUCCESS
