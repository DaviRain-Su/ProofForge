---
id: wsm-081
scope: wasm
status: done
depends-on: [wsm-080]
---

# wsm-081 SDK Card.addSelfEsc 与冻结门托管：XrplHinge

## objective

SDK helpers `Card.addSelfEsc` / `subSelfEsc` / `addSelfSupp` /
`subSelfSupp` / `setSelfDueAhead` (else-if, not `&&`) plus freeze-gated
escrow:

- `latch` moves 5 onto contract `esc`
- frozen caller `latch` / `unlatch` returns 5
- `unfreeze` then unlatch returns esc

Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Public still host -22 and tefBAD_AUTH.

digest `fa616b8741dc3412` (`Examples.XrplHinge`). Registry count 50.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 hinge 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 50）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/hinge.sh`：credit 10；freeze latch 拒 5；unfreeze latch A=5 esc=5；freeze unlatch 拒 5；unfreeze unlatch A=10 esc=0
