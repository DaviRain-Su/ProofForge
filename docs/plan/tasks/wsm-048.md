---
id: wsm-048
scope: wasm
status: done
depends-on: [wsm-047]
---

# wsm-048 takeFrom 源卡是参数三叶

`takeFrom(w0,w1,w2,amount)` 用 `storeOwnerLimbs w0 w1 w2` 切到源卡，
不再钉死 genesis。跟 `pay` 同一套 rewrite。不是 Map。

`Examples.XrplMint` digest `689a550500e28c84`。

活网：B 对 B 自己的卡 `takeFrom` 状态码 1（无 allw）；对 A 仍成功。
