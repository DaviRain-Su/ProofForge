---
id: wsm-085
scope: wasm
status: done
depends-on: [wsm-084]
---

# wsm-085 本地 mint cap 双向锁：XrplCleft

## objective

SDK `Card.flushCapLit` / `Card.selfSuppFitsCapLit` plus Clamp dual
timelock with mint cap:

- `setCap10` writes `cap=10` on the minter card
- third `credit` past cap returns 1
- paused lockIn returns 4; frozen cancel/cashB return 5
- after due only B cashB +192

Uses else-if, not `&&`. Not Rust `?`. Zero wasm params.
Not `Sdk.Payments`. Not `Sdk.Map`. Public still host -22 and tefBAD_AUTH.

digest `c52ef3441e5af62a` (`Examples.XrplCleft`). Registry count 54.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 cleft 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 54）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/cleft.sh`：credit 10；setCap10；credit 拒 1；pause lockIn 拒 4；unpause lockIn；freeze cancel 拒 5；unfreeze cancel；lockIn；freeze B cash 拒 5；unfreeze early B cash 拒 1；A cash 拒 3；late A cancel 拒 1；B cashB +192
