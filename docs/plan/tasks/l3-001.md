---
id: l3-001
scope: shape
status: done
depends-on: [l2-004]
---

# l3-001 init 写全字段 + view 任意叶子

## objective

`init` 的 `paramCount` 按 λ 个数算，不再写死 1。`{ left, right }` 抽出两条 `returnState`，emit 按槽写回，不再把第二槽起清零压成一条。view 可返回任意已布局叶子（`getRight`）。旧 `init (left)` 仍只写第一槽、其余清零。

## path

`SolanaLean/Extract.lean`, `Emit.lean`, `Examples/Pair.lean`, `Tests/*`, `runtime-tests/solana/tests/pair.rs`

## verification

- `initBoth 3 9` Mollusk：left=3，right=9
- `getRight` 返回 right，不改账户
- 旧 `initialize(u64)` / `getLeft` / Counter 不回退
