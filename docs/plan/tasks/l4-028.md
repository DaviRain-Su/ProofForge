---
id: l4-028
scope: runtime
status: done
depends-on: [l4-027]
---

# l4-028 tokenSyncNative

## objective

`tokenSyncNative` 是普通 Lean 包装，抽出成已有 `invoke`。

- 外层 0 owner s+w（prelude）、1 native token 账户 w、2 Token
  内层 metas：native token 账户 w
  data `u8le(17)`（packed SyncNative）
- 不改 TokenAcc / TokenMint 账户表；新例子 TokenNative
- 负例：缺 signer → Custom(1)
- 不再复制发射器

## path

`Ops.lean`, `Runtime.lean`, `Extract.lean`, `Golden.lean`,
`Examples/Svm/TokenNative.lean`, `Tests/*`, `runtime-tests/solana/tests/token_native.rs`

## verification

- TokenNative Mollusk：给 native token 账户加 lamports 后 SyncNative，amount 跟上
- 缺 signer → Custom(1)
- TokenMint2 digest 不回退

## result

- digest `5bc920f79c3711f0`；syncNative `dataLen=1`
- Mollusk：native 账户 amount = 多余 lamports；缺 signer Custom(1)
