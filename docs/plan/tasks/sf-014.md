---
id: sf-014
scope: svm-formalization
status: done
depends-on: [sf-000]
plan: ../svm-formalization-plan.md
---

# sf-014 Account / Memory / Sysvar / Telemetry L1

## 目标

薄查询 facade 的 L1 收口；无持久模型的标 `n/a`。

## 交付

1. `Account.Handle.wellFormed` / word / dataWord 界定理 — **done**
2. `Memory.Span` 几何与非重叠前置（pure）— **done**（wellFormed 分解、overlaps 对称、abut 不重叠）
3. Sysvar / Telemetry：API 形状钉死 + 矩阵 **n/a-L2** — **done**

## 验收

SF-9a → done。
