---
id: wsm-059
scope: wasm
status: done
depends-on: [wsm-058]
---

# wsm-059 本地程序卡 `Card.storeSelf`（XrplVault）

## objective

Persist Owner := contract AccountID (`sfContractAccount`) and write a
shared `supp` slot on that card. Not a Map. Not a Payment.

Local transia/alphanet 2.6.1 funded Create: pokeSelf / `storeSelf` is
green. Public AlphaNet 3.3.0 `home_le_field(524320)` write is still
host **-22**.

digest `6b6e2791d63443d8` (`Examples.XrplVault`). Registry count 28.

| SDK | unfolds to |
|---|---|
| `Card.storeSelf` | `storeOwnerLimbs selfW0 selfW1 selfW2` |

Bedrock/local host field: `sfContractAccount` = ACCOUNT/25 = **524313**.
Not dangell/Bedrock 524315, not public 3.3.0 524320.

`State.bal` stays the caller's card. After writing contract `supp`,
`Card.restoreCaller` so persist does not copy `$bal` onto the vault.

## 不做什么

- 不开公开 `Sdk.Map` / `Sdk.Payments`（公开仍 -22 / -196）
- 不发明 PDA / `setUserData`
- 不把本地 2.6.1 绿当成公开 3.3.0 绿

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 28）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/vault.sh`：local nid≠21337；caller.bal=5 contract.supp=5
