---
id: wsm-051
scope: wasm
status: done
depends-on: [wsm-050]
---

# wsm-051 pay 切回 caller 再 debit

`peekHaltLit` 会把 persist owner 切到 minter。`pay` 用
`peekOwnerLimbs caller` debit，不用入口 `s.bal`。不是 Map。

`Examples.XrplMint` digest `2d97bbe9498468dd`。

活网：B `pay(A,1)` → A.bal=2 B.bal=6 supp=8。
