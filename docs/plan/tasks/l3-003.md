---
id: l3-003
scope: shape
status: done
depends-on: [l3-002]
---

# l3-003 单字段用户 inductive 作 tag+payload

## objective

两构造子、其中一个带一个 `UInt64` 字段的用户 `inductive`，按 Option 同形展开：`name_tag` + `name_p0`。无字段构造子编号 0，有字段构造子编号 1。`match` 抽成 `ite (eq tag 0)`；有字段支读 payload。超过一个字段、超过两个构造子 fail closed。

## path

`ProofForge/Extract.lean`, `Examples/Svm/Choice.lean`, `Tests/*`, `runtime-tests/solana/tests/choice.rs`

## verification

- Choice slots：`pick_tag` 8、`pick_p0` 16
- `getHeld` empty → 0；hold 77 → 77
- 多字段 inductive 仍 fail closed
- 已有合约 Mollusk 不回退
