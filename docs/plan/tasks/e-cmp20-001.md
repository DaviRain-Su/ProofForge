---
id: e-cmp20-001
scope: evm
status: done
depends-on: [e-immut-001]
---

# e-cmp20-001 Addr20 整值相等

## objective

身份比不再手拆三叶。Yul 把两份 Addr20 pack 成 address word 再 `eq`。

- 新增 `evmEq20 a b : Bool`。宿主返回 `true`。抽出六叶：`a.w0/w1/w2`、`b.w0/w1/w2`。
- Yul：pack 两边，`eq(packedA, packedB)`。高 12 字节已清零。
- `Examples.Ownable.bump` 改成 `if evmEq20 evmCaller20 s.owner then …`
- Anvil：owner bump 仍过；非 owner 仍 revert。
- SVM 拒新叶；Legacy adapter 拒新叶。

## 不做

`Addr20` 排序 / `<`；嵌套 `Option Addr20`；把所有现有三叶 `=` 自动改写（只开显式 `evmEq20`）。
