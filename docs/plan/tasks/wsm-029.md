---
id: wsm-029
scope: wasm
status: done
depends-on: [wsm-023]
---

# wsm-029 读 AccountRoot.Balance（协议对象第一片包装）

## objective

合约层包装主账本对象，不是重写。P0：读调用者的 XRP 余额。

AlphaNet XLS-0102：

1. `tx_field(sfAccount)` → 20B AccountID
2. `accountroot_id(ptr,20, out,32)` → ledger index
3. `cache_le(index,32, 0)` → slot
4. `le_field(slot, Balance=393218, out,48)` → STAmount（XRP 通常 8 字节）

AlphaNet 探针已绿（合约 `rGwbAuD3tK9Gd1JNLTj5Nmf5ysr1ixQAvb`）：
poke tesSUCCESS / vmReturnCode=0，槽 `drops` 为 STAmount 前 8 字节
（`4611786026558387880`，不是解码后的 drops 十进制）。

host 存在。下一刀才是 Runtime 叶 + 解码 XRP drops。不开假 SDK。

## 不做什么

- 不改别人余额、不发 Payment（那是 wsm-030）
- 不抄 EVM `balance(address)`
- 不进 CI

## path

`runtime-tests/xrpl/fixture/probe-balance.wat`，`probe-balance.sh`

## verification

- ContractCreate 含 `accountroot_id` / `cache_le` / `le_field`
- poke 对创世账户返回 tesSUCCESS，槽 `drops` 非 0
