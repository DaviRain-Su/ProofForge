---
id: wsm-035
scope: wasm
status: done
depends-on: [wsm-034]
---

# wsm-035 给别人写卡片 + tx Sequence/Fee + amm_id

## 注资（先回答「铸字」）

不是铸币。是给合约**伪账户打 XRP**。公共 AlphaNet **就是测试网**（21337）。
卡住的是这版 `3.3.0-rc1` 预检：Create `InstanceParameters` / Call AMOUNT
`tfSendAmount` 都 **temMALFORMED**。普通 Payment 打合约 = tecNO_PERMISSION。
`emit_built_txn` = -196。**不是**「没上测试网」。

## 活网新事实

| 探针 | 结果 |
|---|---|
| `set_data_object_field` 填别人 20B AccountID | **绿**（`rHP6J9BTFoYz4CAP53Q7Z94ogYQMiRSNN3`，对方槽 `bal=1`） |
| `tx_field(Sequence=131076)` | **绿**（tseq=106） |
| `tx_field(Fee=393224)` | **绿**（STAmount 前 8 字节非 0） |
| `amm_id` import | **host 在**；零 issue → **-15 INVALID_PARAMS**。不开 Sdk.Amm |

给别人写卡片 **不需要** `setUserData` 这个名字。Owner 就是传入的 AccountID。
仍不是任意 KV Map：key 还是编译期 JSON 名，账户必须已存在（探针先 pay 了第二把钥匙）。

## Runtime

`xrplTxSequence` / `xrplTxFeeDrops`。`Examples.XrplTx` digest `2a9d4e10cd7ecec9`。

## 不做什么

- 不开 Sdk.Map / Sdk.Payments / Sdk.Amm
- 不把写别人卡片当成「任意地址 mapping」
