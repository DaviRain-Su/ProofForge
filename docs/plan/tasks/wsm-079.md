---
id: wsm-079
scope: wasm
status: done
depends-on: [wsm-078]
---

# wsm-079 本地冻结门托管：XrplFrost

## objective

Else-if escrow plus per-card freeze:

- `latch` moves 5 onto contract `esc`
- frozen caller `latch` / `unlatch` returns 5
- `unfreeze` then unlatch returns esc

Uses `Card.callerUnlocked` / `Gate.ok`. Not `&&`. Not Rust `?`.
Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Public still host -22 and tefBAD_AUTH.

digest `28539392e2861945` (`Examples.XrplFrost`). Registry count 48.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 frost 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 48）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/frost.sh`：credit 10；freeze latch 拒 5；unfreeze latch A=5 esc=5；freeze unlatch 拒 5；unfreeze unlatch A=10 esc=0
