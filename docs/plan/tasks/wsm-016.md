---
id: wsm-016
scope: wasm
status: done
depends-on: [wsm-015]
---

# wsm-016 XRPL-SDK-PAUSE：零参数 Pausable（XrplHold）

## objective

对标 EVM `Sdk.Pausable`，不学它的 UInt8 / event / Revert selector。

- `Xrpl.Sdk.Pausable.running = 0`、`paused = 1`、`isRunning`（未知 flag fail closed）
- wasm v0 钉死 `errorNamed "paused"` → i32 状态码 **4**（1 overflow、2 div0、3 unauthorized）
- `Examples.Xrpl.XrplHold`：零参数 Ownable + pause。活网同一把钥匙：
  initialize → bump（value=1）→ pause → bump（4，value 不变）→ unpause → bump（value=2）

不新 host。pause 槽是应用 `State` 里的 UInt64，不是 Runtime 叶。

## 不做什么

- 不接 EVM `Pausable.violation` UInt64 / LOG
- 不把 live AlphaNet 放进 CI
- 不做两步移交、不接 Payment

## path

`ProofForge/Wasm/{IR,Emit}.lean`，`ProofForge/Wasm/Xrpl/{Sdk,Registry,Host}.lean`，
`Examples/Xrpl/XrplHold.lean`，`Tests/XrplSpec.lean`，`runtime-tests/xrpl/{check.sh,hold.sh}`

## verification

- `#pf_xrpl_build Examples.Xrpl.XrplHold` digest `e99965ac007e0da8`
- WAT 含 `(i32.const 4)`、export `pause` / `unpause`，不含 wasm i64 参数
- `lake exe pf -- deploy XrplHold` 后上述六步（缺 RPC skip，不把 skip 当绿）
