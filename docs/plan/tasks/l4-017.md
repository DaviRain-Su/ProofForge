---
id: l4-017
scope: runtime
status: done
depends-on: [l4-016]
---

# l4-017 createProgramAddress

## objective

`checkPda bump` 是普通 Lean 名，抽出成 `sol_create_program_address`。

- 种子钉死 `"vault"`；bump 是已抽出的 `Val`
- 成功返回 0，失败返回 1（官方 syscall 语义）
- 完整 32B 地址仍 fail closed
- 负例：bump 0 必须返回 1
- 加在现有 Pda 例子上，不新开程序

## path

`Ops.lean`, `Runtime.lean`, `Extract.lean`, `Emit.lean`, `IR.lean`, `Golden.lean`, `Examples/Pda.lean`, `Tests/*`, `runtime-tests/solana/tests/pda.rs`

## verification

- Pda Mollusk：`check` + canonical bump → 0；`checkBad` + bump 0 → 1
- `findPda` digest 不回退
