---
id: wsm-036
scope: wasm
status: done
depends-on: [wsm-035]
---

# wsm-036 编译期 AccountID 写别人卡片

公共 RPC 已绿，AccountID 用三个 UINT64 叶（little-endian 0..7 / 8..15 / 16..19）。
探针已证：`set_data_object_field` 填对方 20B 就能写。`Context.storeOwnerLimbs`
经 `xrplStoreOwner` 在 persist 前覆盖 mem[0..19]。

`Examples.Xrpl.XrplSend` digest `a8e5e47454812f03`。destination 三个 UINT64 + amount。
`credit(w0,w1,w2,7)` 把 7 写到第二把钥匙的卡片。

还探针：

| 探针 | 结果 |
|---|---|
| `set_data_nested_object_field` | **绿**（`rw1kcqAoJE16yGfvoT6LUhcust5keB42bx`） |
| `trustline_id` | **host 在**；零账户 → **-16 INVALID_ACCOUNT**。不开 Sdk.Trustline |

不开 Sdk.Map。动态 AccountID 是三个 UINT64，不是一个 20 字节参数。
