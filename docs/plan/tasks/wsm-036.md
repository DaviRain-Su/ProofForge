---
id: wsm-036
scope: wasm
status: done
depends-on: [wsm-035]
---

# wsm-036 编译期 AccountID 写别人卡片

公共 RPC 仍 502，不能把 AccountID 当参数。探针已证：
`set_data_object_field` 填对方 20B 就能写。本刀把 40 hex 字面量接到
`xrplAccountLitW0/W1/W2`，发射器在 persist 前覆盖 mem[0..19]。

`Examples.XrplSend` digest `64eb128e0be5a2c6`。destination =
`d0bc2a540b15411f44a24dfb58d23ad5d9d9b350`（第二把钥匙）。

还探针：

| 探针 | 结果 |
|---|---|
| `set_data_nested_object_field` | **绿**（`rw1kcqAoJE16yGfvoT6LUhcust5keB42bx`） |
| `trustline_id` | **host 在**；零账户 → **-16 INVALID_ACCOUNT**。不开 Sdk.Trustline |

不开 Sdk.Map。动态 AccountID 参数仍 502。
