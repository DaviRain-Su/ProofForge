---
id: p-001
scope: extract
status: done
depends-on: [r1-011]
---

# p-001 Extract.lean 三段拆分（Lexical / Decode）

## objective

`ProofForge/Extract.lean`（8665 行）沿既有层缝拆成三个模块，降低安全关键路径
（子集准入 + 抽出）的单文件复杂度，不再继续增长为巨石：

| 模块 | 内容 | 规模 |
|---|---|---|
| `ProofForge/Extract/Lexical.lean` | Expr 前置助手（peel / subst / sketch / 类型谓词），82 个定义 | 902 行 |
| `ProofForge/Extract/Decode.lean` | `asVal` / decode 值与 op 区域，182 个定义；对主文件接口仅 6 个名字 | 6364 行 |
| `ProofForge/Extract.lean` | schema / extract 表面 | 1438 行 |

拆分是纯机械变换：8349 个规范化行逐一与原文比对一致，唯一改动是 76 处
`private` 前缀移除（跨模块引用所需）。

## verification

- `lake build`（280 jobs 全绿，含 `#guard` elaboration 期断言）
- `lake build Tests` 全绿（digest fixture 不变）
- `lake exe pf -- build --target svm` 与 `--target evm` 全量程序构建成功
- `scripts/check_ownership.py` 通过