---
id: wsm-097
scope: wasm
status: done
depends-on: [wsm-096]
---

# wsm-097 本地 pause-gated take 再兑 XRP：XrplRamp

## objective

A `grant` writes `allw=5`. Wallet B `takeB` then `cashSelf` +192:

- ungranted take returns 1
- paused take returns 4
- after grant, takeB A=5 B=5 allw=0; paused B cash returns 4; B cashSelf B=0 supp=5 +192-fee

Else-if, not `&&`. Not Rust `?`. Zero wasm params.
Not `Sdk.Payments`. Not `Sdk.Map`. Public still host -22 and tefBAD_AUTH.

digest `3eaeb4e07e501d84` (`Examples.XrplRamp`). Registry count 65.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 ramp 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 65）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/ramp.sh`：上列断言
