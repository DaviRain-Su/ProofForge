---
id: l1-002
scope: extract
status: done
depends-on: [l1-001]
---

# l1-002 任意 ite + checked 四则

## objective

Extract 认 `=` `≠` `<` `≤` `>` `≥`。假支不必是 overflow：两支都能 decode 就收成 `Op.ite`。

checked mul / div / mod 与 add/sub 同纪律：

- mul：`y = 0` 得 0；否则 `x ≤ u64Max / y` 才乘
- div / mod：`y = 0` → overflow

wrapping 入口仍 fail closed。view 上的 `if` 可返回两个 UInt64。

`Examples.Counter` 增加 `scale` / `divide` / `modulo` / `nonzero`。未登记 disc 仍 fail closed；这四个挂进登记表。

## path

`SolanaLean/Ops.lean`, `Extract.lean`, `Emit.lean`, `IR.lean`, `Examples/Counter.lean`, `Tests/*`, `runtime-tests/solana/tests/counter.rs`

## verification

- extract scale/divide/modulo/nonzero
- wrappingMul fail closed
- Counter Mollusk：原 6 测仍绿；scale 5×3=15；×0=0；溢出保持；divide 8/3=2；/0 保持；modulo 8%3=2；nonzero(0)=1
