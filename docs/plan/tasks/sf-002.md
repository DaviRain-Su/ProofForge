---
id: sf-002
scope: svm-formalization
status: done
depends-on: [sf-001]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-002 Queue pop / peek / initialize / 往返（收口）— done

## 已交付（本分支）

1. `mQueuePop_wrap_advance_links`：head = cap → next = 1
2. `mQueuePop_clear_readback` / `mQueuePop_advance_readback` /
   `mQueuePop_wrap_advance_readback`：pop 读回 / size 效应
3. `mQueuePeek` + `mQueuePeek_empty` / `mQueuePeek_eq`
4. `mQueueInitialize` + `mQueueInitialize_zero_headers`
5. `mQueuePush_pop_roundtrip_empty`（空队列单元素往返）
6. 矩阵 Queue 行 + SF-1b → **done**

## 早已在 main（勿重复）

- `mQueuePop_clear_links` / `mQueuePop_advance_links`（head ≠ cap）
- 通用 `u64toNatSub` 无下溢桥

## 非目标（仍成立）

不证 RbMap；不扩 Ops/Emit。

## 验收

`lake build ProofForge.Svm.Sdk.StorageModel` 绿；同计划 §5 公共门；
Queue 行为可 `#check` 引用。
