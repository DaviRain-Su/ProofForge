---
id: sf-002
scope: svm-formalization
status: doing
depends-on: [sf-001]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-002 Queue pop / peek / initialize / 往返（收口余量）

## 已在 main 落地（勿重复劳动）

- `mQueuePop_clear_links`（size=1 → remaining=0、head=0）
- `mQueuePop_advance_links`（size≥2 且 head≠cap → head+1）
- 通用 `u64toNatSub` 无下溢桥

## 本片仍要交

1. `mQueuePop_wrap_advance_links`：head=cap → next=1
2. pop **读回** / size 效应（clear 与 advance）
3. `mQueuePeek_eq` / empty peek 哨兵
4. `mQueueInitialize_zero_headers`（若 SDK initialize 只写两 header）
5. `mQueuePush_pop_roundtrip`（空或单元素先钉）
6. 矩阵 Queue 行 + SF-1b → done

## 非目标

不证 RbMap；不扩 Ops/Emit。

## 验收

同计划 §5 公共门；Queue 行为在 Tests 或 `#check` 可引用。
