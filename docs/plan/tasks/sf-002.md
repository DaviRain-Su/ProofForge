---
id: sf-002
scope: svm-formalization
status: done
depends-on: [sf-001]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-002 Queue pop / peek / initialize / 往返（收口余量）

## 已在 main 落地（勿重复劳动）

- `mQueuePop_clear_links`（size=1 → remaining=0、head=0）
- `mQueuePop_advance_links`（size≥2 且 head≠cap → head+1）
- 通用 `u64toNatSub` 无下溢桥

## 本片交付（2026-09-01）

1. `mQueuePop_wrap_advance_links`：head=cap → next=1
2. pop **读回**：`mQueuePop_clear_readback` / `mQueuePop_advance_readback` / `mQueuePop_wrap_advance_readback`
3. `mQueuePeek` / `mQueuePeek_empty` / `mQueuePeek_eq`
4. `mQueueInitialize` / `mQueueInitialize_zero_headers`
5. `mQueuePush_pop_roundtrip_empty`
6. 矩阵 SF-1b → done

## 非目标

不证 RbMap；不扩 Ops/Emit。

## 验收

- `lake build ProofForge.Svm.Sdk.StorageModel` 绿
- Queue 行为定理可 `#check` 引用
