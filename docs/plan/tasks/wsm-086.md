---
id: wsm-086
scope: wasm
status: done
depends-on: [wsm-085]
---

# wsm-086 本地 clawB 双向锁：XrplClip

## objective

SDK `Card.addBalLit` / `Card.subBalLit` / `Card.peekBalLit` plus freeze-gated
dual timelock with claw:

- `sendToB` moves 5 onto wallet B
- minter `clawB` takes it back and cuts `supp`
- frozen cancel/cashB return 5
- after due only B cashB +192

Uses else-if, not `&&`. Not Rust `?`. Zero wasm params.
Not `Sdk.Payments`. Not `Sdk.Map`. Public still host -22 and tefBAD_AUTH.

digest `55c5cfabd75805bc` (`Examples.XrplClip`). Registry count 55.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 clip 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 55）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/clip.sh`：credit 15；sendToB A=10 B=5 supp=15；clawB B=0 supp=10；lockIn；freeze cancel 拒 5；unfreeze cancel；lockIn；freeze B cash 拒 5；unfreeze early B cash 拒 1；A cash 拒 3；late A cancel 拒 1；B cashB +192
