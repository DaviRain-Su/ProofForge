---
id: wsm-010
scope: wasm
status: done
depends-on: [wsm-009]
---

# wsm-010 XRPL-SDK-ACCESS：`requireOwner`

## objective

见 [xrpl-sdk-gap.md](../analysis/xrpl-sdk-gap.md)「现在就能做」。
对标 EVM `Access.requireOwner`，不学它的 `Revert.unauthorized(address)`。

```lean
@[pf_inline] def requireOwner (owner : AccountId) : Bool :=
  AccountId.eq Context.caller owner
```

`Examples.XrplOwn.bump` 走这个门。不新增 host。失败仍是 `.error .unauthorized` → 状态码 3。

## path

`ProofForge/Wasm/Xrpl/Sdk.lean`，`Examples/XrplOwn.lean`，Registry digest（IR 形状若变则重钉）

## verification

- `#pf_xrpl_own_emit_check` 仍要三个 `i64.eq` 和 `(i32.const 3)`
- `own.sh`：owner bump 改 value；非 owner 返回 3
