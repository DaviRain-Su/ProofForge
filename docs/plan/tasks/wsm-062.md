---
id: wsm-062
scope: wasm
status: done
depends-on: [wsm-061]
---

# wsm-062 本地编译期目的地 Payment（`Pay.emitToLit`）

## objective

Arity-0 Runtime leaf `xrplEmitPayToLit hex` → Payment of 192 drops to a
compile-time 20-byte AccountID. SDK `Pay.emitToLit`. Not `Sdk.Payments`.

Local 2.6.1 cannot sign Function.ParameterType, so dest is a hex
literal (wallet B `d0bc2a54…b350`). Public AlphaNet pokeEmit is still
**-196**.

digest `e722061475dea65e` (`Examples.XrplGift`). Registry count 31.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开 `Sdk.Payments`
- 不发明运行时 AccountID 参数
- 不把本地 B+=192 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 31）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/gift.sh`：local nid≠21337；B XRP +192
