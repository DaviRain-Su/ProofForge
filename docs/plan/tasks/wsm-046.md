---
id: wsm-046
scope: wasm
status: done
depends-on: [wsm-045]
---

# wsm-046 XrplMint setCap 不能低于 supp

非零 `cap` 必须 `≥ supp`。`0` 仍是不限额。不是 Map。

`Examples.XrplMint` digest `b099c860d71c8d9`。

活网：`setCap(8)` 在 `supp=9` 时状态码 1，`cap` 仍 9；`setCap(0)` 成功，`cap=0`。
