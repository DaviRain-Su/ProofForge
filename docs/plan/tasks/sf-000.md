---
id: sf-000
scope: svm-formalization
status: done
depends-on: [p-005]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-000 证明基础设施成文 — done

## 交付

在 `ProofForge/Svm/Sdk/StorageModel.lean` Queue 证明区固化并在后续片复用：

1. **多写读回路径**：`mReadField_write_same` / `mReadField_write_other` 拼装；
   `mQueuePushAt_twoWrites` / `mQueuePopAt_twoWrites` 为三写/两写入口
2. **wf 入口**：`queue_wf_parts` / `mQueueCapacityFacts`；禁止深 `.1.1.1` 投影
3. **UInt64 桥**：`u64toNatAdd` / `u64toNatSub`；`ite` 不透明时走词级 / `if_neg`/`if_pos`，
   不硬 `simp [reduceIte]`；`toNat = 0 → = 0` 用 `UInt64.toNat_inj`
4. **链接再读回**：先 `*_links` 把 SDK 控制流缩到 `*At`，再复用 `*At_twoWrites`

## 非目标

未改 SDK `@[pf_inline]` 控制流；未新增业务容器定理（Queue 收口在 sf-001/002）。

## 验收

- `lake build ProofForge.Svm.Sdk.StorageModel`
- `python3 scripts/check_no_sorry.py`
- `python3 scripts/check_ownership.py`
