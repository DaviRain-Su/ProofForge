---
id: sf-000
scope: svm-formalization
status: todo
depends-on: [p-005]
plan: ../svm-formalization-plan.md
---

# sf-000 证明基础设施成文

## 目标

把 p-005 已验证的证明习惯写成可复用约定，降低后续 Queue/BitSet/Map 片的重复踩坑。

## 交付

1. 在 `ProofForge/Svm/Sdk/StorageModel.lean` 顶部或独立小节固化：
   - `mWriteWord_self` / `mWriteWord_other` 为多写读回的标准拼装路径
   - `*_wf_parts` 为 wellFormed 入口；禁止深 `.1.1.1` 投影
   - UInt64 `ite` 不透明时改走词级引理，不硬 `simp [reduceIte]`
2. （可选）抽出 2–4 个通用辅助引理到明显分区，供 Queue/Vec 复用
3. 更新 `docs/plan/svm-formalization-plan.md` §6：SF-0 → done

## 非目标

不新增业务容器定理；不改 SDK `@[pf_inline]` 控制流。

## 验收

- `lake build ProofForge`
- `python3 scripts/check_no_sorry.py`
- `python3 scripts/check_ownership.py`
