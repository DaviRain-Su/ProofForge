---
id: wsm-047
scope: wasm
status: done
depends-on: [wsm-046]
---

# wsm-047 pause 挡住 setCap / approve

`setCap` 现在跟 mint/pay/burn/`takeFrom`/`approve` 一样看 minter 卡上的
`halt`。暂停后状态码 4，卡不变。不是 Map。

`Examples.XrplMint` digest `3be69fa4437cd198`。

活网：pause 后 `setCap(9)` / `approve(1)` 状态码 4。
