---
id: wsm-074
scope: wasm
status: done
depends-on: [wsm-073]
---

# wsm-074 本地受益人领取：claimB after due

## objective

XrplVest plus a compile-time beneficiary:

- SDK `Card.selfDueReached` (`due ≤ ledgerSqn`)
- A `lockIn` moves 5 onto contract `esc`, `due = ledgerSqn + 2`
- only wallet B `claimB` after due
- A calling claimB returns 3; early B claim returns 1

Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Not XRPL EscrowCreate. Public still host -22 and tefBAD_AUTH.

digest `4857c33431f624cb` (`Examples.XrplClaim`). Registry count 43.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 AccountID 参数
- 不把本地 claim 写成公开活网验收
- 不叫 XRPL 原生 EscrowFinish

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 43）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/claim.sh`：credit 10；lockIn A=5 esc=5；立刻 B claim 拒 1；A claimB 拒 3；下一账本 B claimB B=5 esc=0 A=5
