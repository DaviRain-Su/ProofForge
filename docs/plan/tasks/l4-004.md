---
id: l4-004
scope: runtime
status: done
depends-on: [l4-003]
---

# l4-004 账户 0 AccountInfo 只读叶子

## objective

把 Loader V3 账户 0 已经 walk 到的五元组暴露成普通 Lean 名。只读，不改 lamports / data / owner。

- `accLamports0` → `ACC0_LAMPORTS`
- `accOwner0` → `ACC0_OWNER+0` 首 u64（不是完整 32B）
- `accDataLen0` → `ACC0_DATA_LEN`
- `accN` → `NUM_ACCOUNTS`
- `isSigner0` / `isWritable0` / `isExecutable0` → header +1/+2/+3，0 或 1

`accKey0` 本切片仍是 `signerKey0` 的别名（首 u64）。完整 32B、账户 1、改余额 fail closed。

## path

`Ops.lean`, `IR.lean`, `Extract.lean`, `Emit.lean`, `Runtime.lean`, `Golden.lean`, `Examples/Info.lean`, `Tests/*`, `runtime-tests/solana/tests/info.rs`

## verification

- Info Mollusk：读余额 / owner 首 u64 / data_len / NUM_ACCOUNTS / 三旗
- 只读入口不改账户数据
- Clock / Transfer / Ping 不回退
