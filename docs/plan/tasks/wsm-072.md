---
id: wsm-072
scope: wasm
status: done
depends-on: [wsm-071]
---

# wsm-072 本地托管：lockIn / releaseToB / refund

## objective

New JSON key `esc` on the current Owner card (arity 1/3, same host as `lock`):

- Runtime `xrplFlushEsc` / `xrplPeekEsc`
- SDK `Context.flushEsc` / `peekEscLimbs` / `Card.peekSelfEsc`
- caller `lockIn` moves 5 onto contract `esc`
- minter `releaseToB` pays B from `esc`
- caller `refund` takes `esc` back
- paused lockIn returns 4; empty refund returns 1

Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`. Not a PDA.
Public still host -22 and tefBAD_AUTH.

digest `2d5fcf19a07dfde1` (`Examples.XrplHoldEsc`). Registry count 41.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 AccountID 参数
- 不把本地 escrow 写成公开活网验收
- 不叫 XRPL 原生 EscrowCreate

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 41）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/holdesc.sh`：credit 10；pause lock 拒 4；lockIn A=5 esc=5；releaseToB B=5 esc=0；lockIn；refund A=5 esc=0；再 refund 溢 1
