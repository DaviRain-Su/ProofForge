---
id: wsm-077
scope: wasm
status: done
depends-on: [wsm-076]
---

# wsm-077 SDK Gate.and2 / Gate.ok；XrplLatch

## objective

wasm v0 rejects `bitAnd` / `bitOr`, so entries cannot use `&&` / `||`.
SDK names that still extract as nested `if`:

- `Gate.u64Max` (named def, not `~~~(0)` in an entry body)
- `Gate.and2` / `Gate.and3` (nested `if`, not `Bool.and`)
- `Gate.ok v` (`v ≤ u64Max`) to sequence `flush*` / `storeOwner`

`XrplLatch` is HoldEsc credit/latch/unlatch written with those names.
Sequential `flush*` stay nested `if Gate.ok` — those are effects, not AND.

Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Public still host -22 and tefBAD_AUTH.

digest `b4df97f27b0d2f3d` (`Examples.XrplLatch`). Registry count 46.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不在 wasm v0 开 `&&` / `bitAnd`
- 不把顺序 `flush*` 收成一个 AND（会打乱 persist Owner）
- 不开公开门面

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 46）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/latch.sh`：credit 10；latch A=5 esc=5；unlatch A=10 esc=0；再 unlatch 溢 1
