---
id: wsm-061
scope: wasm
status: done
depends-on: [wsm-060]
---

# wsm-061 本地可变 drops Payment（`Pay.emitToCallerDrops`）

## objective

Arity-1 Runtime leaf `xrplEmitPayDrops drops` → STAmount
`0x4000000000000000 | drops`, Payment to the caller. SDK
`Pay.emitToCallerDrops`. Not `Sdk.Payments`.

Local 2.6.1 cannot sign Function.ParameterType, so the example uses a
compile-time 384. Public AlphaNet pokeEmit is still **-196**.

digest `7e760f9ff6b668e6` (`Examples.XrplTip`). Registry count 30.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md) — 本地先做，公开先放.

## 不做什么

- 不开公开 `Sdk.Payments`
- 不发明运行时 UINT64 参数（本地 2.6.1 签不了）
- 不把 384 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 30）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/tip.sh`：local nid≠21337；ping tesSUCCESS/0
