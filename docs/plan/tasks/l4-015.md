---
id: l4-015
scope: runtime
status: done
depends-on: [l4-014]
---

# l4-015 Memo write

## objective

`memoWrite` 是普通 Lean 包装，抽出成已有 `invoke`。

- 外层：账户 0 s（prelude 仍要求 writable）、1 Memo 程序
- 内层 metas：账户 0 signer、只读
- 内层 data：`ascii "ok"`（2B，无 tag）
- 负例：缺账户 0 signer
- 不做动态字符串；不再复制发射器

## path

`Ops.lean`, `Runtime.lean`, `Extract.lean`, `Golden.lean`, `Examples/Svm/Memo.lean`, `Tests/*`, `runtime-tests/solana/tests/memo.rs`

## verification

- Memo Mollusk：CPI 进官方 Memo v3，成功；缺 signer → Custom(1)
- TokenAcc digest 不回退
