---
id: wsm-067
scope: wasm
status: done
depends-on: [wsm-066]
---

# wsm-067 本地资金池 cap + operator（XrplFund）

## objective

XrplPool plus:

- `setCap10` writes minter-card `cap=10`; further `credit` returns overflow (1)
- `grantOp` writes minter-card `allw=1`; wallet B may `pause`/`unpause`
- B pause before grant returns unauthorized (3)

Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Public still host -22 and tefBAD_AUTH.

digest `8cc80156ad30a85c` (`Examples.XrplFund`). Registry count 36.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 cap/operator 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 36）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/fund.sh`：B pause 拒 3；grantOp；B pause/unpause；credit 10；setCap10；credit 溢 1；sendToB；cashToB B.xrp+=192
