---
id: l5-002
scope: phoenix
status: doing
depends-on: [l5-001]
---

# l5-002 Phoenix 限价 / TIF / 部分成交 / UInt64 费用

## objective

Phoenix 吃单看 `askPrice` 和 `expiry`。部分成交 = `min(want, sizes[i])`。费用按 UInt64 bps，不是官方 u128。

循环扫书等 l5-001 抽出器能区分循环 binder 后再收。本切片仍用 4 路 `ite`。

## path

`Projects/Phoenix.lean`, `Tests/PhoenixSpec.lean`, `Golden.lean`

## verification

- `limit < askPrice` overflow
- `unixTime ≥ expiry`（expiry≠0）overflow
- `want > sizes[i]` 仍成交 `sizes[i]`
- `#pf_build Projects.Phoenix` 绿
