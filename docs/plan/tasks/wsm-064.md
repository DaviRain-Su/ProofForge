---
id: wsm-064
scope: wasm
status: done
depends-on: [wsm-063]
---

# wsm-064 本地暂停门金库（XrplBank）

## objective

Compose Access + Pausable + program-card `supp` + `Pay.emitToCaller`.
Minter pause writes `halt` on the genesis card. Paused `credit` / `cash`
return status 4 and leave balances. Unpause then `cash` burns 5/5 and
emits 192 drops.

Not `Sdk.Payments`. Not `Sdk.Map`. Public AlphaNet still -22 / -196.

digest `6a344e3db8cdf235` (`Examples.XrplBank`). Registry count 33.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明 PDA / 任意 KV
- 不把本地暂停金库写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 33）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/bank.sh`：credit 5/5；pause；credit/cash=4；unpause；cash 0/0
