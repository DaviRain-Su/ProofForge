---
id: sf-004
scope: svm-formalization
status: done
depends-on: [sf-003]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-004 Versioned 状态机 — done

## 已交付（`ProofForge/Svm/Sdk/VersionedModel.lean`）

1. L1：`header_wf_parts` / `transition_wf_parts` + disc/ver 词公式
2. 纯 classify：uninitialized / ready / wrongDisc / unsupported / malformed_mixed
3. 模型：`mVersionedInspect` / `mVersionedInitialize` / `mVersionedApply`
4. Initialize：全零 → initialized+ready；alreadyReady 幂等；wrongDisc 拒绝不写
5. Apply：fromVersion → transitioned（写 version、disc 不变）；alreadyCurrent 幂等
6. 矩阵 SF-2b → **done**

## 非目标（仍成立）

多边迁移图、payload 变换、租约/关户。

## 验收

`lake build ProofForge.Svm.Sdk.VersionedModel`；no_sorry / ownership 绿。
