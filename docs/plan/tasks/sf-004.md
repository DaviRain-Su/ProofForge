---
id: sf-004
scope: svm-formalization
status: todo
depends-on: [sf-003]
plan: ../svm-formalization-plan.md
---

# sf-004 Versioned 状态机

## 目标

为 `Sdk/Versioned.lean` 补 L1 wf + L2 在 AccountWords 上的 classify / initialize / apply。

## 交付

1. `Header.wellFormed` / `Transition.wellFormed` 的 `*_wf_parts`
2. 模型：`mVersionedClassify` / `mVersionedInitialize` / `mVersionedApply` 与 SDK 同构
3. 定理：未初始化 → initialize → ready；错误 disc/version fail-closed 不写；单边 Transition apply 成功/拒绝

## 非目标

多边迁移图、payload 变换、租约/关户。

## 验收

SF-2b → done。
