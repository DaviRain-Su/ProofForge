---
id: sf-004
scope: svm-formalization
status: done
depends-on: [sf-003]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-004 Versioned 状态机

## 目标

为 `Sdk/Versioned.lean` 补 L1 wf + L2 在 AccountWords 上的 classify / initialize / apply。

## 本片交付（2026-09-01）

1. `Header.wellFormed_parts` / `Transition.wellFormed_parts`
2. 纯 `Header.classify_*` 分支定理（uninit/ready/malformed/wrong/unsupported）
3. 模型 `ProofForge/Svm/Sdk/VersionedModel.lean`：
   - `mVersionedRead` / `mVersionedClassify` / `mVersionedInitialize` / `mVersionedApply`
   - `mVersionedInitialize_from_zero` / `_alreadyReady` / `_rejected_noop`
   - `mVersionedApply_transitioned`
4. 矩阵 SF-2b → done

## 非目标

多边迁移图、payload 变换、租约/关户。

## 验收

- `lake build ProofForge.Svm.Sdk.Versioned ProofForge.Svm.Sdk.VersionedModel` 绿
