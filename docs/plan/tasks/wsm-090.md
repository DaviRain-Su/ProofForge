---
id: wsm-090
scope: wasm
status: done
depends-on: [wsm-089]
---

# wsm-090 本地 allowance 进托管：XrplCrate

## objective

A `grant` writes `allw=5`. Wallet B `takeAndLock` cuts A's `allw`+`bal`
into contract `esc` with `due = ledgerSqn + 2`. A cancel before due.
After due only B cashB +192.

Else-if, not `&&`. Not Rust `?`. Zero wasm params.
Not `Sdk.Payments`. Not `Sdk.Map`. Public still host -22 and tefBAD_AUTH.

digest `65b2ce389bb8434` (`Examples.XrplCrate`). Registry count 59.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 crate 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 59）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/crate.sh`：credit 10；B takeAndLock 拒 1；grant；B takeAndLock A=5 allw=0 esc=5；cancel A=10 esc=0；grant；takeAndLock；early B cash 拒 1；A cash 拒 3；late A cancel 拒 1；B cashB +192
