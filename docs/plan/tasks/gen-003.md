---
id: gen-003
scope: emit
status: done
depends-on: [gen-002]
---

# gen-003 按 Val 生成 load

## objective

increment/init/get 的 load 由抽出的 `Val` 决定：`.field _ "value"` → 账户 `ACC0_DATA+8`；`.arg _` → `INSTRUCTION_DATA+8`。对调 `checkedAddU64` 的左右操作数必须改变第一条 load 来源。

## context

docs/modules/ops.md, docs/modules/emit.md

## path

ProofForge/Emit.lean, Tests/EmitSpec.lean

## verification

`IR.extractedCounter` 仍可汇编且 Mollusk 4/4；左右对调的夹具汇编先 load instruction data。
