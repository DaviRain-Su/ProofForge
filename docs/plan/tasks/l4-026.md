---
id: l4-026
scope: runtime
status: done
depends-on: [l4-025]
---

# l4-026 systemTransferWithSeed

## objective

`systemTransferWithSeed` 是普通 Lean 包装，抽出成已有 `invoke`。

- 外层 0 base s+w（prelude）、1 派生付款账户 w、2 收款账户 w、3 System
  内层 metas：派生付款 w / base s / 收款 w
  data `u32le(11) || u64le lamports || u64le(5) || ascii "vault" || programId`
  （bincode TransferWithSeed；种子钉死 `"vault"`；from_owner = 当前 program id）
- 派生地址 = `create_with_seed(base, "vault", program_id)`
- 不改 Transfer / SysSeed 账户表；新例子 SysXfer
- 负例：缺 base signer → Custom(1)；错派生地址 → System Custom(5)
- 不再复制发射器

## path

`Ops.lean`, `Runtime.lean`, `Extract.lean`, `Golden.lean`,
`Examples/SysXfer.lean`, `Tests/*`, `runtime-tests/solana/tests/sys_xfer.rs`

## verification

- SysXfer Mollusk：从 `create_with_seed(acc0, "vault", program)` 转 lamports 到 acc2
- 缺 signer → Custom(1)；错地址 → System Custom(5)
- SysSeed / TokenSize digest 不回退

## result

- digest `906efee37227cb35`；sendSeed `dataLen=57`
- Mollusk：派生账户转出 lamports；缺 signer Custom(1)；错地址 System Custom(5)
