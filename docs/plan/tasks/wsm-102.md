---
id: wsm-102
scope: wasm
status: done
depends-on: [wsm-100]
---

# wsm-102 探针 AMM 读路径（不是 Sdk.Amm）

## objective

`amm_id` host 已在（零 issue **-15**）。本刀走完 id → `cache_le` → `le_field`。
**不开 Sdk.Amm，不发 AMMCreate。**

## 结果（2026-08-31，公开 3.3.0-rc1 / 21337）

- 20-byte 非 XRP issue → poke **-15** INVALID_PARAMS
- 40-byte STIssue（20B currency + 20B issuer）后 **`amm_id` 绿**，写出 32B id
- `cache_le` **-10**（AlphaNet 上没有该 USD/EUR AMM SLE）
- `le_field(LPTokenBalance=393247)` 未跑到（cache 已负返回）
- 合约 `rHikcq2tbCgZGfTSEY6qysv4Jmb1L4NLKS`

下一刀 Runtime 要先有一条真实 AMM（`AMMCreate`，节点交易）。sfield：
LPTokenBalance 393247，TradingFee 65541。Issue 非 XRP 必须 40 字节。

## 不做什么

- 不开 `Sdk.Amm` / WASM Uniswap
- 不把 -10 当报价 0
- 本刀不发 AMMCreate

## path

`runtime-tests/xrpl/fixture/probe-amm.wat`，`probe-amm.sh`

## verification

- Create tesSUCCESS（三 import 都在）
- poke `tecBYTECODE_REJECTED` / **-10**（缺 AMM SLE）
