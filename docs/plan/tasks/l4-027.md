---
id: l4-027
scope: runtime
status: done
depends-on: [l4-026]
---

# l4-027 tokenInitMint

## objective

`tokenInitMint` 是普通 Lean 包装，抽出成已有 `invoke`。

- 外层 0 authority s+w（prelude）、1 mint w、2 Token
  内层 metas：mint w
  data `u8le(20) || u8le(6) || accKey 0 || u8le(0)`
  （packed InitializeMint2；decimals 钉死 6；mint_authority = acc0；freeze = None）
- 不改 TokenMint 账户表；新例子 TokenMint2
- 负例：缺 signer → Custom(1)
- 不再复制发射器

## path

`Ops.lean`, `Runtime.lean`, `Extract.lean`, `Golden.lean`,
`Examples/TokenMint2.lean`, `Tests/*`, `runtime-tests/solana/tests/token_mint2.rs`

## verification

- TokenMint2 Mollusk：未初始化 82B mint 写成 decimals=6、authority=acc0、is_initialized
- 缺 signer → Custom(1)
- SysXfer digest 不回退

## result

- digest `89ae474933102cb4`；openMint `dataLen=35`
- Mollusk：未初始化 mint 写成 decimals=6、is_initialized；缺 signer Custom(1)
