---
id: l4-005
scope: runtime
status: done
depends-on: [l4-003]
---

# l4-005 表层通用 invoke

## objective

抽出认编译期钉死的 `invoke`，不再为每条 recipe 加名字。

- Lean 表面：`invoke programIx metas data`，`programIx` / metas / data 布局在抽出时已知。
- `systemTransfer` / `invokeAcc1` 是普通 Lean 包装，展开成同一条 `invoke`。
- Transfer / Ping 抽出结果与现在相同；digest 不变。
- 运行时 program id、变长 remaining accounts、动态 data 仍 FC。
- `invokeSigned` 本切片不做。

## path

`Runtime.lean`, `Extract.lean`, `Examples/Svm/Transfer.lean`, `Examples/Svm/Ping.lean`, `Tests/*`

## verification

- `#pf_build` Transfer / Ping digest 不变
- Transfer / Ping Mollusk 不回退
- 未注册的动态 `invoke` fail closed
