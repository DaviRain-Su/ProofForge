---
id: wsm-075
scope: wasm
status: done
depends-on: [wsm-074]
---

# wsm-075 本地受益人兑 XRP：cashB after due

## objective

XrplClaim plus emit:

- A `lockIn` moves 5 onto contract `esc`, `due = ledgerSqn + 2`
- only wallet B `cashB` after due: burns `esc`+`supp`, emits 192 drops to B
- A calling cashB returns 3; early B cash returns 1

Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Not XRPL EscrowFinish. Public still host -22 and tefBAD_AUTH.

digest `4d769f5622556277` (`Examples.XrplPayout`). Registry count 44.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 AccountID 参数
- 不把本地 payout 写成公开活网验收
- 不叫 XRPL 原生 EscrowFinish

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 44）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/payout.sh`：credit 10；lockIn A=5 esc=5；立刻 B cash 拒 1；A cashB 拒 3；下一账本 B cashB esc=0 supp=5 A=5 B 净得 192 drops（扣 1e6 Call 费）
