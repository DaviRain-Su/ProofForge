---
id: sf-007
scope: svm-formalization
status: partial
depends-on: [sf-006]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-007 Bytes + Record64 + WideVec

## Landed in TransientModel

- Bytes: begin/push/appendLe64 + range/no-room/stale; Vec↔Bytes scratch isolation
- Record reject paths (no room → no partial)
- **`mRecordAppend2_readback`**: two-limb success path (both words + length +2)
- **`mRecordAppend3_readback`** / **`mRecordAppend4_readback`**: three/four-limb success readback
- **`mVec64Set_readback`** / **`mVec64Set_get`**: in-bounds set + get readback
- **`mVec128Push_readback`** / **`mVec256Push_readback`**: Vector128/256 push success (via append2/4)
- **`mVec128Set_limb_readback`**: single-limb set within a Vector128 record
- **`mVec256Push_first_rejected`**: four-limb no-room reject (mirrors Vector128)
- **`mVec64Push_preserves_prior`** / **`mVec128Push_get`**: get-after-push for both Vector128 limbs
- **`mVec256Push_get`**: get-after-push for all four Vector256 limbs
- **Bytes get/set/pop model**: `mBytesGet` / `mBytesSet` / `mBytesPop` with push/set/pop readback + empty/range/oob rejects

## Still open

- Bytes `logData` coverage (emitter-side; model is host-opaque)

## Acceptance

SF-4b → done for Bytes/Record/WideVec model theorems; logData remains emitter-side.
