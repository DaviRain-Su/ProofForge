---
id: sf-007
scope: svm-formalization
status: partial
depends-on: [sf-006]
plan: ../svm-formalization-plan.md
updated: 2026-09-01
---

# sf-007 Bytes + Record64 + WideVec

## Landed in TransientModel

- Bytes: begin/push/appendLe64 + range/no-room/stale; Vec↔Bytes scratch isolation
- Record reject paths (no room → no partial)
- **`mRecordAppend2_readback`**: two-limb success path (both words + length +2)
- **`mRecordAppend3_readback`** / **`mRecordAppend4_readback`**: three/four-limb success readback

## Still open

- Vector128/256 whole-value get/set/push success
- Bytes pop/set/logData coverage

## Acceptance

SF-4b → done when success paths above are complete.
