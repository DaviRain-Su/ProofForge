---
id: sf-007
scope: svm-formalization
status: done
depends-on: [sf-006]
plan: ../svm-formalization-plan.md
---

# sf-007 Bytes + Record64 + WideVec

## 目标

在 TransientModel 上覆盖 `TransientBytes` / `TransientRecord64` / `TransientWideVec`。

## 交付

1. Bytes：push/pop/set/appendLe64/logData 守卫；两 byte slot 与两 vec slot 共存非干涉
2. Record64：append1..4 整记录预检；残缺前缀不写
3. Vector128/256：委托 Record；整值 get/set/push 读回

## 验收

SF-4b → done。
