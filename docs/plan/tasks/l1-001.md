---
id: l1-001
scope: profile
status: done
depends-on: [fld-002]
---

# l1-001 属性入口 + 按名 disc + 多 mutate

## objective

`@[solana_entry]` 标记可编译根。种类从返回类型推断：structure → init；`Except …` → mutate；`UInt64` → view。`#solana_build Ns` 收同一名字空间下的入口，不再手写三 ident。

discriminator = PF 公式，按 **链上方法名** 算。Lean `init` 映射为 `initialize`（命令关键字）。同一程序可编 increment 与 decrement，不再共用 increment disc。

未登记的方法名 fail closed（与 layout marker 相同：先算 SHA-256 再挂表）。

## path

`SolanaLean/Attr.lean`, `IR.lean`, `Extract.lean`, `Emit.lean`, `Commands.lean`, `Examples/*`, `Tests/*`, `runtime-tests/solana/tests/*.rs`

## verification

- `#solana_build Examples.Counter` 抽出 init/increment/decrement/get
- Counter.so 含 decrement disc；Mollusk 原 4/4 仍绿，并加 decrement 成功 / 下溢保持
- Pair.so 用 `creditLeft` / `getLeft` disc；Mollusk 4/4
- 无 `@[solana_entry]` 的名字空间 fail closed
