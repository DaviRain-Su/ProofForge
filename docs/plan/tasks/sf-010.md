---
id: sf-010
scope: svm-formalization
status: done
depends-on: [sf-009]
plan: ../svm-formalization-plan.md
---

# sf-010 StorageEnumerableSet

## 目标

EnumerableSet = values[] + 索引 map；证 initialize/contains/insert/remove/valueAt 模型代数。

## 交付

1. Descriptor `wf_parts`（`StorageEnumerableSetModel.lean`）
2. 抽象 `EnumSet`：empty contains；insert 满/重复 fail-closed；empty→insert→contains/valueAt 往返
3. remove 后 absence；`BoundedSet.canValueAt` 在 wf count 下归结为 `index < count`
4. 反向索引用 `AssocIndex` 建模；key4 RB 结构保持 → **sf-011**

## 验收

SF-6 → done（索引/前缀层；RB → sf-011）。
