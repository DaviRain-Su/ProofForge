---
id: sf-010
scope: svm-formalization
status: todo
depends-on: [sf-009]
plan: ../svm-formalization-plan.md
---

# sf-010 StorageEnumerableSet

## 目标

EnumerableSet = values[] + 索引 map；证 initialize/contains/insert/remove/valueAt 模型代数。

## 交付

1. Descriptor wf_parts
2. insert 后 contains ∧ valueAt 往返；满/重复 fail-closed
3. remove 后 swap-remove 政策与 Core.Collections 位置律一致（若已绑定）

## 验收

SF-6 → done。
