---
id: wsm-068
scope: wasm
status: done
depends-on: [wsm-067]
---

# wsm-068 本地 treasury：clawback / burn / cashSelf

## objective

XrplFund plus:

- `clawB` minter claws 5 from wallet B and cuts `supp`
- `burn` caller burns 5 own points and cuts `supp`
- `cashSelf` burns 5 and emits 192 drops to the caller

Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Public still host -22 and tefBAD_AUTH.

digest `4ace63cdcef0446b` (`Examples.XrplTreasury`). Registry count 37.
Create on 2.6.1 rejects 14-export Functions (`temARRAY_TOO_LARGE`); this
program keeps 7 exports. Pause/cap/operator stay on `XrplFund`.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 AccountID 参数
- 不把本地 treasury 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 37）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/treasury.sh`：credit 10；sendToB A=5 B=5；clawB B=0 supp=5；burn A=0 supp=0；credit；cashSelf A 净得 192 drops（扣 1e6 Call 费）
