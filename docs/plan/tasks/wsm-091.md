---
id: wsm-091
scope: wasm
status: done
depends-on: [wsm-090]
---

# wsm-091 本地 burnFrom 双向锁：XrplCinder

## objective

A `grant` writes `allw=5`. Wallet B `burnFrom` cuts A's `allw`+`bal` and
contract `supp` without crediting B. Then dual timelock: A `lockIn`,
cancel before due, B cashB after due +192.

Else-if, not `&&`. Not Rust `?`. Zero wasm params.
Not `Sdk.Payments`. Not `Sdk.Map`. Public still host -22 and tefBAD_AUTH.

digest `e1e45412ebb2dcdd` (`Examples.XrplCinder`). Registry count 60.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 cinder 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 60）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/cinder.sh`：credit 15；B burnFrom 拒 1；grant；B burnFrom A=10 allw=0 supp=10 B bal missing/0；lockIn；cancel；lockIn；early B cash 拒 1；A cash 拒 3；late A cancel 拒 1；B cashB +192
