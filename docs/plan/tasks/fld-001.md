---
id: fld-001
scope: layout
status: done
depends-on: [gen-004]
---

# fld-001 多字段 UInt64 state

## objective

`IR.Program` 带 `fields : Array String`。账户布局 = 8 字节 header + 每字段 8 字节 LE，按数组顺序。抽出认任意 `.field` 投影；发射按字段名算 `ACC0_DATA` 偏移。`Examples.Pair`（left/right）与 Counter 共用管道。

## path

ProofForge/IR.lean, Extract.lean, Emit.lean, Examples/Pair.lean, Tests/*

## verification

Counter 不回退。Pair.init/creditLeft 抽出成功。未知字段 fail closed。
