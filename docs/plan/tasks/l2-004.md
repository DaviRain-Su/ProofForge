---
id: l2-004
scope: layout
status: done
depends-on: [l2-001]
---

# l2-004 无 payload 枚举作 tag

## objective

用户 `inductive` 无参数构造子（Lean `isEnumType`）收成一叶 `name`，宽 8，`u64-le`。构造子按声明顺序编号 0…n-1。`mode = .idle` 写成字面量；`s.mode = .live` 抽成 `eq (field mode) (lit k)`。带参数构造子 fail closed。Bool 仍不是叶子。

## path

`ProofForge/Extract.lean`, `IR.lean`, `Examples/Phase.lean`, `Tests/*`, `runtime-tests/solana/tests/phase.rs`

## verification

- Phase slots：`mode` 偏移 8
- 带 payload 的 inductive fail closed
- Counter / Pair / Flag / Maybe / Window Mollusk 不回退
- Phase Mollusk：init idle=0；setLive 写 1；isLive 读 tag
