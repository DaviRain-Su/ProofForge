---
id: l4-021
scope: runtime
status: done
depends-on: [l4-020]
---

# l4-021 slotsPerEpoch

## objective

`slotsPerEpoch` 是普通 Lean 叶子，抽出后发射 `sol_get_epoch_schedule_sysvar`，读 `EpochSchedule.slots_per_epoch`（偏移 0）。

- 33 字节 `repr(C)` 缓冲；只暴露第一个 u64
- 宿主 `@[irreducible]` stub，值是 0
- `warmup` / `first_normal_*` 仍 fail closed
- 完成定义：Mollusk 默认 schedule 返回 432000；改 `slots_per_epoch` 后再读一次，证明不是 ELF 常量

## path

`Runtime.lean`, `Ops.lean`, `Extract.lean`, `Emit.lean`, `IR.lean`,
`Golden.lean`, `Examples/Epoch.lean`, `Tests/EpochSpec.lean`,
`runtime-tests/solana/tests/epoch.rs`

## verification

- `span` 默认返回 432000；改 schedule 后返回新值
- TokenAuth digest 不回退
