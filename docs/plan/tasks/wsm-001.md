---
id: wsm-001
scope: wasm
status: done
depends-on: [evm-001]
---

# wsm-001 WASM 第三 profile：XRPL Bedrock 方言 v0 竖切

## objective

按 [research/06-wasm-feasibility.md](../../research/06-wasm-feasibility.md) 路线 B
（第三个 profile）落第一刀，绑定 XRPL Bedrock（XLS-0101）：
`Core.Target.Registration` 拒绝 svm/evm 叶；v0 子集 fail closed（UInt64 槽、
scalar UInt64 参数、checked 五则、ite、exit ops；loop / local / vector / map /
named error / 位运算全拒）；artifact 是 scaffold-xrp 方言 Rust 源
（`xrpl_wasm_std` `get_data` / `set_data`、`#[unsafe(no_mangle)] pub extern "C"`），
zero-tool，`deployable=false`。digest 域 `wasm-xrpl|`，与 SVM / EVM 故意不同。
XRPL 事实（crate git rev、wasm32-unknown-unknown、AlphaNet≠主网、无 rustc Tool
Lock）来自旧仓 proof_forge 的 ADR-0049/0050 研究。

mutating entry 返回 `i32` 状态码（错误码钉死：1 overflow/underflow、2
divide-by-zero），源声明的 public 返回值省略（读 view）；view 返回 `-> u64`
（FFI-safe 标量，多值 fail closed）。

## path

`ProofForge/Wasm/{Ops,IR,Emit,Registry,Assemble,Commands}.lean`,
`ProofForge/Cli.lean`, `Tests/WasmSpec.lean`,
`runtime-tests/wasm/check.sh` + stub crate, `scripts/check_artifact_manifest.py`,
`scripts/check_ownership.py`, `.github/workflows/ci.yml`

## verification

- `#pf_wasm_build Examples.Counter`：digest `335b688107a04afc` 与 Registry 钉死
- `#pf_wasm_reject Examples.Clock`（svm 叶）、`#pf_wasm_reject Examples.EvmCtx`（evm 叶）
- 发射锚点检查：`checked_add/sub/mul → ok_or(1i32)`、`checked_div/rem → ok_or(2i32)`、
  `wrapping_*` guard 算术、Bedrock 存储助手与导出形状
- `pf build --target wasm --out build/wasm` 写 `Counter.rs`（digest 行）；
  `runtime-tests/wasm/check.sh`：生成源对本地 stub crate `cargo check` 通过
- `check_artifact_manifest.py --self-test` 覆盖 wasm registry（count 1）