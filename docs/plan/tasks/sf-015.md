---
id: sf-015
scope: svm-formalization
status: done
depends-on: [sf-000]
plan: ../svm-formalization-plan.md
---

# sf-015 Token / ATA / Pda / System / Memo 扫尾

## 目标

把已有 L1 切片补到「无缺口清单」。

## 交付

1. Token AccountState/MintState 守卫完备定理表 — **done**（6 个 exact `iff_guards`）
2. ATA Create + RecoverNested 全部 role `< L` — **done**（7 + 8 个角色）
3. Pda/System ASCII wf 全路径；Memo bound — **done**（ASCII 字符路径、512-byte 直接界）
4. Pubkey/Program 若仍缺对称性/委托等式则补齐 — **done**（Pubkey 传递/账户对称、Program 委托等式）

## 非目标

CPI 宿主成功性、Token-2022 extension 全语义。

## 验收

SF-9b → done。
