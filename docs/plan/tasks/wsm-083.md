---
id: wsm-083
scope: wasm
status: done
depends-on: [wsm-082]
---

# wsm-083 本地暂停+冻结双向锁：XrplClamp

## objective

SDK `Card.flushHaltLit` plus pause+freeze dual timelock:

- `lockIn` moves 5 onto contract `esc` with `due = ledgerSqn + 5`
- paused lockIn/cancel/cashB return 4
- frozen A `cancel` / frozen B `cashB` return 5
- unpause+unfreeze then A cancel before due; after due only B cashB +192

Uses `Card.callerUnlocked` / `Card.flushCallerLock` / `Card.flushHaltLit`.
Not `&&`. Not Rust `?`. Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Public still host -22 and tefBAD_AUTH.

digest `d10847e4aee069a2` (`Examples.XrplClamp`). Registry count 52.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 clamp 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 52）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/clamp.sh`：credit 10；pause lockIn 拒 4；unpause lockIn；freeze cancel 拒 5；unfreeze cancel；lockIn；freeze B cash 拒 5；unfreeze early B cash 拒 1；A cash 拒 3；late A cancel 拒 1；B cashB +192
