---
id: l4-029
scope: runtime
status: done
depends-on: [l4-028]
---

# l4-029 账户 1 只读叶子

## objective

把 AccountInfo 只读叶子从账户 0 扩到账户 1。不是新 CPI recipe。

- Lean：`accLamports1` / `accOwner1` / `accDataLen1` / `isSigner1` / `isWritable1` / `isExecutable1`
- 抽出成运行时叶子；完整 32B key 仍 FC
- 读到账户 1 叶子就走 N 账户 walk（至少 2），**不**因此强制 acc0 signer+writable
- `usesCpi` 仍只表示有 `invoke`；walk 另算
- 不改 Info 单账户布局；新例子 Peer
- 现有 acc0 只读 view 不回退
- 负例：只给 1 个账户 → Custom(1)

## path

`Ops.lean`, `Runtime.lean`, `Extract.lean`, `IR.lean`, `Emit.lean`,
`Golden.lean`, `Examples/Info.lean`, `Tests/*`, `runtime-tests/solana/tests/info.rs`

## verification

- Info Mollusk：账户 1 的 lamports / owner 首 u64 / data_len / 三旗
- 单账户调账户 1 叶子 → Custom(1)
- acc0 叶子、TokenNative digest 不回退

## result

- Peer digest `db69bdf1efa1011d`；`usesWalk`、非 CPI；`call` 不是 `ja`
- Info digest 仍是 `a4fe01bf75fee0c`
- Mollusk：账户 1 lamports / owner / data_len / 三旗；缺第二账户 Custom(1)
