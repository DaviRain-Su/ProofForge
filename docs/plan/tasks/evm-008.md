---
id: evm-008
scope: evm
status: done
depends-on: [evm-007]
---

# evm-008 独立 EVM runtime 叶子

## objective

新 Lean 名，不翻译 SVM：

- `evmCaller` → `and(caller(), 2^64-1)`（地址末 8 字节，不是完整 address，不是 `signerKey0`）
- `evmBlockNumber` → `number()`，`> UInt64.max` 则 revert（不是 `clockSlot`）

SVM 发射器碰到这两叶 fail closed。`#pf_build Examples.EvmCtx` 必须失败。`#pf_evm_build` 成功。Anvil：`caller()` 对上发送者低 8 字节；`height()` 对上 `cast block-number`。

## path

`ProofForge/Runtime.lean`, `Ops.lean`, `Extract.lean`, `Emit.lean`, `Evm/Emit.lean`, `Examples/EvmCtx.lean`, `runtime-tests/evm/anvil_ctx.sh`

## verification

- `#pf_evm_build Examples.EvmCtx` 绿
- `#pf_build Examples.EvmCtx` → `svm rejects evm leaf`
- `#pf_evm_build Examples.Clock` 仍拒 SVM 叶子
- `runtime-tests/evm/anvil.sh` 含 ctx
