---
id: wsm-096
scope: wasm
status: done
depends-on: [wsm-095]
---

# wsm-096 本地 takeB + clawB 再兑 XRP：XrplRaid

## objective

A `grant` writes `allw=5`. Wallet B `takeB`, minter `clawB`, A `cashSelf` +192:

- ungranted take returns 1
- frozen B take returns 5
- after grant, takeB A=5 B=5 allw=0; B claw returns 3; A claw B=0 supp=5; frozen A cash returns 5; A cashSelf A=0 supp=0 +192-fee

Else-if, not `&&`. Not Rust `?`. Zero wasm params.
Not `Sdk.Payments`. Not `Sdk.Map`. Public still host -22 and tefBAD_AUTH.

digest `fec942260f477144` (`Examples.XrplRaid`). Registry count 64.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 raid 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 64）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/raid.sh`：上列断言
