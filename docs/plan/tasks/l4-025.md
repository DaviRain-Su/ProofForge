---
id: l4-025
scope: runtime
status: done
depends-on: [l4-024]
---

# l4-025 systemAssignWithSeed

## objective

`systemAssignWithSeed` 是普通 Lean 包装，抽出成已有 `invoke`。

- 外层 0 base s+w（prelude）、1 派生账户 w、2 System
  内层 metas：派生账户 w / base s
  data `u32le(10) || accKey 0 || u64le(5) || ascii "vault" || programId`
  （bincode AssignWithSeed；种子钉死 `"vault"`）
- 派生地址 = `create_with_seed(base, "vault", program_id)`
- 挂在 SysSeed 上；cpiAccountCount 仍是 3
- 负例：缺 base signer → Custom(1)；错派生地址 → System Custom(5)
- 不再复制发射器

## path

`Ops.lean`, `Runtime.lean`, `Extract.lean`, `Golden.lean`,
`Examples/Svm/SysSeed.lean`, `Tests/*`, `runtime-tests/solana/tests/sys_seed.rs`

## verification

- SysSeed Mollusk：把 `create_with_seed(acc0, "vault", program)` 的 owner 改成当前 program
- 缺 signer → Custom(1)；错地址 → System Custom(5)
- TokenSize digest 不回退

## result

- digest `490cec59af518f0c`；assignSeed `dataLen=81`
- Mollusk：派生账户 owner=program；缺 signer Custom(1)；错地址 System Custom(5)
