---
id: l4-007
scope: runtime
status: done
depends-on: [l4-006]
---

# l4-007 invokeSigned 一组种子

## objective

`invoke` 带一组编译期钉死的 signer seeds。一条 ASCII 种子 + 一个 bump 叶。

- Lean：`invokeSigned programIx metas data seed bump`
- 抽出成 `Op.invoke … seeds`；`invoke` 是 seeds 为空的特例
- 发射：`SolSignerSeed[2]` + `SolSignerSeeds[1]`，`r4/r5` 不再是 0
- 例子：CPI 到账户 1 上的 stub，PDA 用 `"vault"` + `findPda "vault"` 签字
- 负例：错 bump → syscall 失败
- 多组 seeds / 运行时拼种子本切片 FC

## path

`Ops.lean`, `IR.lean`, `Extract.lean`, `Emit.lean`, `Runtime.lean`, `Golden.lean`, `Examples/Signed.lean`, `Tests/*`, `runtime-tests/solana/tests/signed.rs`

## verification

- Transfer / Ping / Call digest 不变（空 seeds）
- Signed Mollusk：正确 bump 成功；错 bump 失败
