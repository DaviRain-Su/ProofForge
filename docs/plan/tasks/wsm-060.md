---
id: wsm-060
scope: wasm
status: done
depends-on: [wsm-059]
---

# wsm-060 本地 `Pay.emitToCaller`（XrplEmit）

## objective

Runtime leaf `xrplEmitPay` → `build_txn` / `add_txn_field` / `emit_built_txn`
Payment of 192 drops to the caller. SDK name `Pay.emitToCaller`. Not
`Sdk.Payments`.

Local transia/alphanet 2.6.1 funded Create: `ping` tesSUCCESS/0.
Public AlphaNet 3.3.0 pokeEmit is still **tefBAD_AUTH -196**.

digest `5d97e10e9319e9e1` (`Examples.XrplEmit`). Registry count 29.

Imports are added only when a method uses `.emitPay`. Other programs
keep the old host table.

Local NetworkID field is 63456 (`0x0000F7E0`). Public 21337 path is
emitted without that field (official Amount+Destination); it still
returns -196 on the public node.

## 不做什么

- 不开公开 `Sdk.Payments`（公开仍 -196）
- 不开 `Sdk.Map`（公开程序卡仍 -22）
- 不发明 PDA / `submitTransaction`

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 29）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/emit.sh`：local nid≠21337；ping tesSUCCESS/0
