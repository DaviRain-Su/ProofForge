---
id: l5-005
scope: extract
status: done
depends-on: [l2-001]
---

# l5-005 嵌套 structure 摊平

## objective

用户 `structure` 可以嵌套。抽出时摊成扁平槽 `parent_child`。Lean 侧保持组合类型。

- `inferSlots` 递归进用户 structure
- `s.book.price` → `book_price`
- `Vector Nested n` 每个元素再摊
- `extends` 仍关
- 红黑树不是这刀：下一步用节点 structure + `Vector`

## path

`Extract.lean`, `Examples/Nested.lean`, `Golden.lean`

## verification

- `#pf_build Examples.Nested` 槽 `book_price, book_size, baseFree`
- `bestAsk` / `askSize` 读嵌套投影
- `Vector Nested n` 动态叶读写由 Tree 覆盖
- Nested Mollusk 覆盖嵌套 projection 更新
- Pair / Flag / Phoenix digest 不回退
