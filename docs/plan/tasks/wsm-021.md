---
id: wsm-021
scope: wasm
status: done
depends-on: [wsm-018]
---

# wsm-021 探针 `trace_num`（AlphaNet）

## objective

XLS-0102 §5.9：`trace_num(msg_ptr, msg_len, number:i64) → i32`，调试日志，
不是 EVM LOG。活网 `ContractCreate` + `poke` 均 `tesSUCCESS` / `vmReturnCode=0`
（合约 `rzQb334bA7s4sCfxLUPCDbH1yT22jeeMC`）。

**不开 `Sdk.Log`。** 共识不索引这条日志；当工程门断言会碎。

## 不做什么

- 不接线 Runtime 叶 / SDK 名
- 不当 EVM LOG3 / event ABI
- 不进 CI

## path

`runtime-tests/xrpl/fixture/probe-trace.wat`，`runtime-tests/xrpl/probe-trace.sh`

## verification

- `runtime-tests/xrpl/probe-trace.sh`：deploy + initialize + poke → 0
  （缺 RPC skip）
