---
id: wsm-037
scope: wasm
status: done
depends-on: [wsm-036]
---

# wsm-037 nested JSON 槽 `user_bal` → `{user:{bal}}`

AlphaNet `set/get_data_nested_object_field` 探针已绿。发射器：槽名
`user_bal`（字母后缀）走 nested host；`xs_0`（数字后缀）仍扁 key。
Bedrock 没有 nested import，继续扁 `user_bal`。

`Examples.XrplNest` digest `860982785dab0d6d`。`credit(delta)` 经 `function_param`
取一个 UINT64。活网双钱包：A `credit(3)` → A `{user:{bal}}=3`；B `credit(5)` →
B=5 且 A 仍 3。nested JSON 仍是每人一张卡，不是 Sdk.Map。
