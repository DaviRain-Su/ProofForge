---
id: wsm-066
scope: wasm
status: done
depends-on: [wsm-065]
---

# wsm-066 本地资金池（XrplPool）

## objective

One local program composing the green leaves:

- caller cards + contract `supp`
- minter pause (`halt`, status 4)
- per-user freeze (`lock`, status 5)
- internal `sendToB` (compile-time dest, 5 points)
- `cashToB` burns 5 points and emits 192 drops to wallet B

Zero wasm params. Not `Sdk.Payments`. Not `Sdk.Map`.
Public AlphaNet still -22 / -196.

digest `57814a14c17161a5` (`Examples.XrplPool`). Registry count 35.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 AccountID 参数（本地 2.6.1 签不了）
- 不把本地资金池写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 35）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/pool.sh`：A credit 10；sendToB A=5 B=5；freeze send/cash=5；pause=4；cashToB A=0 supp=5 B.xrp+=192
