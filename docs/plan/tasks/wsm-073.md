---
id: wsm-073
scope: wasm
status: done
depends-on: [wsm-072]
---

# wsm-073 本地时间门托管：due ≤ ledgerSqn

## objective

JSON key `due` on the current Owner card (arity 1/3, same host as `esc`):

- Runtime `xrplFlushDue` / `xrplPeekDue`
- SDK `Context.flushDue` / `peekDueLimbs` / `Card.peekSelfDue`
- `lockIn` writes `due = ledgerSqn + 2` and moves 5 onto `esc`
- `refund` requires `due ≤ ledgerSqn`; the next ledger after lockIn still fails (1)

Uses existing `get_ledger_sqn`. Zero wasm params. Not `Sdk.Payments`.
Not `Sdk.Map`. Not XRPL EscrowCreate. Public still host -22 and tefBAD_AUTH.

digest `9ccd6e0b0e597393` (`Examples.XrplVest`). Registry count 42.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 vesting 写成公开活网验收
- 不叫 XRPL 原生 EscrowCreate / Finish

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 42）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/vest.sh`：credit 10；lockIn A=5 esc=5 due≠0；立刻 refund 拒 1；下一账本 refund A=10 esc=0
