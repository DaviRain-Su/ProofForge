---
id: wsm-087
scope: wasm
status: done
depends-on: [wsm-086]
---

# wsm-087 本地 burn 双向锁：XrplClot

## objective

Clip path plus caller `burn`:

- `sendToB` / minter `clawB` as Clip
- frozen `burn` returns 5; unfreeze burn cuts caller `bal` and `supp`
- freeze-gated A cancel / B cashB after due +192

Uses else-if, not `&&`. Not Rust `?`. Zero wasm params.
Not `Sdk.Payments`. Not `Sdk.Map`. Public still host -22 and tefBAD_AUTH.

digest `3b1de560e72e534e` (`Examples.XrplClot`). Registry count 56.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 clot 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 56）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/clot.sh`：credit 20；sendToB；clawB；freeze burn 拒 5；unfreeze burn A=10 supp=10；lockIn；freeze cancel 拒 5；unfreeze cancel；lockIn；freeze B cash 拒 5；unfreeze early B cash 拒 1；A cash 拒 3；late A cancel 拒 1；B cashB +192
