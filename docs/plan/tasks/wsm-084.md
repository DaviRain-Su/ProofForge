---
id: wsm-084
scope: wasm
status: done
depends-on: [wsm-083]
---

# wsm-084 本地 operator pause 双向锁：XrplClasp

## objective

SDK `Card.flushAllwLit` / `Card.allwLitIsOne` plus Clamp dual timelock
with operator grant:

- A `grantOp` writes `allw=1` on the minter card
- wallet B `pause` then lockIn returns 4; A `unpause` restores
- frozen A `cancel` / frozen B `cashB` return 5
- after due only B cashB +192

Uses else-if, not `||` / `&&`. Not Rust `?`. Zero wasm params.
Not `Sdk.Payments`. Not `Sdk.Map`. Public still host -22 and tefBAD_AUTH.

digest `44ca3ba8ccd77e29` (`Examples.XrplClasp`). Registry count 53.

Strategy: [xrpl-local.md](../analysis/xrpl-local.md).

## 不做什么

- 不开公开门面
- 不发明运行时 UINT64 参数
- 不把本地 clasp 写成公开活网验收

## verification

- `lake build Tests.XrplSpec Tests.CliSpec`
- `python3 scripts/check_artifact_manifest.py --self-test`（XRPL count 53）
- `bash runtime-tests/xrpl/check.sh`
- `bash runtime-tests/xrpl/clasp.sh`：credit 10；B pause 拒 3；grantOp；B pause lockIn 拒 4；A unpause lockIn；freeze cancel 拒 5；unfreeze cancel；lockIn；freeze B cash 拒 5；unfreeze early B cash 拒 1；A cash 拒 3；late A cancel 拒 1；B cashB +192
