---
id: evm-007
scope: evm
status: done
depends-on: [evm-006]
---

# evm-007 Darwin / Linux 共用 Anvil 入口

## objective

四个 Anvil 脚本共用 `lib.sh`：只认 Darwin / Linux；按 `FOUNDRY_BIN` → `~/.foundry/bin` → `PATH` 找 `anvil`/`cast`；用本机 `python3` 或 `python`。`runtime-tests/evm/anvil.sh` 一次跑完。缺工具 skip，不当绿。

## path

`runtime-tests/evm/lib.sh`, `anvil.sh`, `anvil_*.sh`, `README.md`, `docs/modules/evm.md`

## verification

- 本机 macOS：`runtime-tests/evm/anvil.sh` 四条绿
- 无 Foundry 时四条都 skip、总入口 exit 0
